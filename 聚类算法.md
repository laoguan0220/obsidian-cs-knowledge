
title: 聚类算法与 K-means 聚类
aliases:
  - K-means
  - K均值聚类
  - 聚类算法
tags:
  - 机器学习
  - 无监督学习
  - 聚类
  - K-means
  - 欧氏距离
date: 2026-07-18
status: learning
---

## 聚类算法与 K-means 聚类

## 一句话理解

聚类是一类[[无监督学习]]方法，它在没有人工类别标签的情况下，根据样本之间的相似程度，自动把相似样本划分到同一组中。

K-means 的核心思想是：

> 预先指定簇的数量 $K$，不断把样本分配给距离最近的聚类中心，再用每个簇中样本的均值更新聚类中心，直到分组稳定。

其基本循环为：

$$
\boxed{
\text{初始化中心}
\rightarrow
\text{分配样本}
\rightarrow
\text{更新中心}
\rightarrow
\text{重复迭代}
}
$$

---

## 摘要

聚类的目标不是根据已有标签预测类别，而是从数据自身的结构中发现分组规律。

K-means 是最经典的划分式聚类算法之一。它假设：

- 样本可以表示成数值向量；
- 样本之间可以用[[欧氏距离]]衡量；
- 每个簇可以用一个均值向量表示；
- 同一个簇中的样本应该尽量靠近该簇中心；
- 不同簇的中心应该在空间中相对分离。

标准 K-means 主要最小化所有样本到其所属聚类中心的**平方欧氏距离之和**：

$$
J
=
\sum_{k=1}^{K}
\sum_{x_i\in C_k}
\left\|x_i-\mu_k\right\|_2^2
$$

其中：

- $K$：需要划分的簇数量；
- $C_k$：第 $k$ 个簇；
- $\mu_k$：第 $k$ 个簇的中心；
- $x_i$：一个样本；
- $\left\|x_i-\mu_k\right\|_2^2$：样本到中心的平方欧氏距离。

K-means 通过交替执行“样本分配”和“中心更新”，不断降低目标函数 $J$，最终得到稳定的聚类结果。

> [!summary]
> K-means 本质上是在寻找 $K$ 个代表点，使每个样本都尽可能靠近其中一个代表点，并让所有样本到各自代表点的平方距离总和尽可能小。

---

## 一、为什么需要聚类

### 没有类别标签的数据仍然可能存在结构

在[[监督学习]]中，训练数据通常具有人工标签，例如：

```text
图像1 → 蜜蜂
图像2 → 黄蜂
图像3 → 蝴蝶
```

模型学习的是：

$$
\text{输入特征}
\rightarrow
\text{已知类别}
$$

但很多实际数据没有人工标签，例如：

- 大量未分类的图像；
- 用户的消费记录；
- 蜜蜂运动轨迹；
- 文本向量；
- 商品特征；
- 遥感目标框尺寸；
- 神经网络提取的特征向量。

此时无法直接进行普通的监督分类，但可以根据样本之间的相似性寻找内部结构。

聚类解决的问题是：

$$
\text{没有标签的数据}
\rightarrow
\text{自动发现若干组相似样本}
$$

---

### 聚类的典型用途

聚类可以用于：

- 用户群体划分；
- 图像分组；
- 文本主题发现；
- 异常样本检测；
- 数据预处理；
- 特征分析；
- 伪标签生成；
- 数据集探索；
- 目标框尺寸统计；
- 图像颜色量化。

在目标检测中，经典 Anchor-based YOLO 模型曾使用 K-means 对数据集中的真实框宽高进行聚类，从而得到具有代表性的 [[Anchor]] 尺寸。

例如，样本可以表示为：

$$
x_i=(w_i,h_i)
$$

K-means 将大量真实框宽高划分成若干组，每组中心可以作为一种先验框尺寸。

> [!note]
> 聚类产生的编号通常没有天然语义。
>
> K-means 得到的“簇0”“簇1”“簇2”只是分组编号，不会自动告诉我们这些簇分别代表“小蜜蜂”“大蜜蜂”或“远处蜜蜂”。这些含义需要人在聚类之后进行解释。

---

## 二、聚类中的样本与特征

### 样本通常表示为向量

假设每个样本具有 $d$ 个特征，则第 $i$ 个样本可以表示为：

$$
x_i=
\left(
x_{i1},
x_{i2},
\dots,
x_{id}
\right)
$$

例如，一只蜜蜂的运动状态可以表示为：

$$
x_i=
(\text{速度},\text{方向变化率},\text{停留时间})
$$

一个目标框可以表示为：

$$
x_i=(w_i,h_i)
$$

一张图像经过神经网络提取后，也可以表示为高维特征向量：

$$
x_i\in\mathbb{R}^d
$$

聚类算法并不是直接理解“蜜蜂”“图片”或“用户”，而是处理这些对象对应的数值特征。

---

### 相似性需要通过距离定义

为了判断两个样本是否相似，需要定义距离或相似度。

常见方式包括：

- [[欧氏距离]]；
- 平方欧氏距离；
- [[曼哈顿距离]]；
- [[余弦相似度]]；
- 马氏距离；
- 汉明距离；
- IoU 距离。

不同距离会产生不同的聚类结果。

标准 K-means 与平方欧氏距离紧密相关，因此它适合：

- 连续数值特征；
- 特征尺度相近；
- 簇接近球状或凸状；
- 簇可以用均值表示的场景。

---

## 三、欧氏距离与平方欧氏距离

### 欧氏距离

对于两个 $d$ 维向量：

$$
x=(x_1,x_2,\dots,x_d)
$$

$$
y=(y_1,y_2,\dots,y_d)
$$

欧氏距离为：

$$
d(x,y)
=
\sqrt{
\sum_{j=1}^{d}
(x_j-y_j)^2
}
$$

也可以写成：

$$
d(x,y)=\|x-y\|_2
$$

---

### 平方欧氏距离

平方欧氏距离省略了开平方操作：

$$
d^2(x,y)
=
\sum_{j=1}^{d}
(x_j-y_j)^2
$$

也可以写为：

$$
d^2(x,y)=\|x-y\|_2^2
$$

由于平方根是单调递增函数，因此：

$$
d(x,a)<d(x,b)
$$

与：

$$
d^2(x,a)<d^2(x,b)
$$

具有相同的大小关系。

因此，寻找最近聚类中心时，使用欧氏距离或平方欧氏距离会得到相同结果。

标准 K-means 通常直接使用平方欧氏距离，因为：

- 不需要计算平方根；
- 计算更加方便；
- 数学推导更简洁；
- 均值恰好是平方距离和的最优中心。

> [!important]
> K-means 不只是“碰巧使用欧氏距离”。
>
> 它的中心更新公式之所以是均值，正是因为目标函数使用了平方欧氏距离。如果更换距离定义，最优中心通常也会发生变化。

---

## 四、K-means 的目标函数

假设有 $n$ 个样本：

$$
X=
\{x_1,x_2,\dots,x_n\}
$$

希望将其划分为 $K$ 个簇：

$$
C_1,C_2,\dots,C_K
$$

每个簇对应一个聚类中心：

$$
\mu_1,\mu_2,\dots,\mu_K
$$

K-means 希望最小化：

$$
J
=
\sum_{k=1}^{K}
\sum_{x_i\in C_k}
\left\|x_i-\mu_k\right\|_2^2
$$

这个目标函数常被称为：

- 簇内平方和；
- Within-Cluster Sum of Squares；
- WCSS；
- SSE；
- Inertia。

目标函数中的每一项表示：

$$
\text{一个样本到其所属簇中心的平方距离}
$$

整个目标函数表示：

$$
\text{所有样本到各自簇中心的平方距离总和}
$$

如果 $J$ 较小，说明同一簇中的点整体更紧密。

---

### K-means 的目标不是最大化不同簇之间的距离

K-means 直接优化的是：

$$
\text{簇内平方距离之和}
$$

而不是直接计算：

$$
\text{不同簇之间的距离}
$$

但当簇内样本更加紧凑时，聚类中心通常也会表现出一定程度的分离。

---

## 五、K-means 的完整工作流程

K-means 需要事先给定簇数量：

$$
K
$$

然后执行以下步骤。

---

### 第一步：初始化 $K$ 个聚类中心

选择：

$$
\mu_1^{(0)},
\mu_2^{(0)},
\dots,
\mu_K^{(0)}
$$

作为初始中心。

最简单的方法是从训练样本中随机选择 $K$ 个点。

例如：

```text
样本集合中随机选择两个点
→ 分别作为簇1和簇2的初始中心
```

初始化会影响最终结果，因为 K-means 通常只能保证收敛到局部最优解。

---

### 第二步：把每个样本分配给最近中心

固定当前聚类中心。

对于每个样本 $x_i$，计算它到所有中心的平方欧氏距离：

$$
\|x_i-\mu_1\|_2^2,
\|x_i-\mu_2\|_2^2,
\dots,
\|x_i-\mu_K\|_2^2
$$

把样本分配给距离最小的中心：

$$
c_i
=
\arg\min_{k\in\{1,\dots,K\}}
\left\|x_i-\mu_k\right\|_2^2
$$

其中 $c_i$ 表示样本 $x_i$ 所属的簇编号。

该步骤可以概括为：

$$
\boxed{
\text{固定中心，重新划分样本}
}
$$

---

### 第三步：重新计算每个簇的中心

固定样本所属的簇。

对于第 $k$ 个簇，重新计算其中所有样本的均值：

$$
\mu_k
=
\frac{1}{|C_k|}
\sum_{x_i\in C_k}x_i
$$

对于二维点，可以分别计算横坐标和纵坐标均值：

$$
\mu_k
=
\left(
\frac{1}{|C_k|}
\sum_{x_i\in C_k}x_{i1},
\frac{1}{|C_k|}
\sum_{x_i\in C_k}x_{i2}
\right)
$$

该步骤可以概括为：

$$
\boxed{
\text{固定分组，重新计算中心}
}
$$

---

### 第四步：重复迭代

不断重复：

$$
\text{分配样本}
\rightarrow
\text{更新中心}
$$

直到满足某个停止条件。

常见停止条件包括：

- 样本所属簇不再变化；
- 聚类中心不再变化；
- 聚类中心移动距离小于阈值；
- 目标函数下降幅度小于阈值；
- 达到最大迭代次数。

---

## 六、为什么聚类中心是均值

这是 K-means 中最关键的数学原因。

假设某个簇中有 $m$ 个一维样本：

$$
x_1,x_2,\dots,x_m
$$

希望寻找一个中心 $\mu$，使所有样本到中心的平方距离之和最小：

$$
J(\mu)
=
\sum_{i=1}^{m}
(x_i-\mu)^2
$$

展开：

$$
J(\mu)
=
\sum_{i=1}^{m}
\left(
x_i^2-2x_i\mu+\mu^2
\right)
$$

对 $\mu$ 求导：

$$
\frac{dJ}{d\mu}
=
-2\sum_{i=1}^{m}x_i
+
2m\mu
$$

令导数等于零：

$$
-2\sum_{i=1}^{m}x_i
+
2m\mu
=
0
$$

得到：

$$
m\mu
=
\sum_{i=1}^{m}x_i
$$

因此：

$$
\boxed{
\mu
=
\frac{1}{m}
\sum_{i=1}^{m}x_i
}
$$

这就是所有样本的算术平均值。

---

### 多维向量的情况

对于多维样本：

$$
x_i\in\mathbb{R}^d
$$

目标函数为：

$$
J(\mu)
=
\sum_{i=1}^{m}
\|x_i-\mu\|_2^2
$$

对向量 $\mu$ 求梯度：

$$
\nabla_\mu J
=
2m\mu
-
2\sum_{i=1}^{m}x_i
$$

令梯度为零：

$$
2m\mu
-
2\sum_{i=1}^{m}x_i
=
0
$$

得到：

$$
\boxed{
\mu
=
\frac{1}{m}
\sum_{i=1}^{m}x_i
}
$$

因此，在平方欧氏距离下，使簇内平方距离和最小的中心就是样本均值。

> [!note]
> 如果目标函数使用绝对距离：
>
> $$
> \sum_i |x_i-\mu|
> $$
>
> 最优中心通常是中位数，而不是均值。
>
> 这说明“使用什么距离”和“使用什么中心”是相互关联的。

相关笔记：

- [[均值]]
- [[中位数]]
- [[欧氏距离]]
- [[梯度]]
- [[偏导数]]

---

## 七、K-means 为什么能够收敛

K-means 的一次完整迭代包含：

1. 分配样本；
2. 更新中心。

这两个步骤都不会让目标函数增大。

---

### 分配步骤不会增大目标函数

固定所有聚类中心。

每个样本都被重新分配给距离最近的中心：

$$
c_i
=
\arg\min_k
\|x_i-\mu_k\|_2^2
$$

假设样本 $x_i$ 原来属于中心 $\mu_a$，重新分配后属于中心 $\mu_b$。

由于 $\mu_b$ 是最近中心：

$$
\|x_i-\mu_b\|_2^2
\leq
\|x_i-\mu_a\|_2^2
$$

因此，每个样本对总损失的贡献都不会增加。

所以：

$$
J_{\text{分配后}}
\leq
J_{\text{分配前}}
$$

---

### 更新中心不会增大目标函数

固定每个簇中的样本。

对于第 $k$ 个簇，新的中心被设置为该簇样本均值：

$$
\mu_k
=
\frac{1}{|C_k|}
\sum_{x_i\in C_k}x_i
$$

均值是平方欧氏距离和的最小点，因此：

$$
J_{\text{更新后}}
\leq
J_{\text{更新前}}
$$

---

### 一轮迭代后的结果

因此：

$$
J^{(t+1)}
\leq
J^{(t)}
$$

目标函数单调不增。

同时：

$$
J\geq0
$$

因为平方距离不可能为负。

因此目标函数不会无限下降到负无穷。

---

### 为什么通常能够在有限步内停止

假设有 $n$ 个样本和 $K$ 个簇。

每个样本都有 $K$ 种可能归属，因此可能的分组方式最多为：

$$
K^n
$$

虽然这个数可能非常大，但它是有限的。

当分组确定后，各簇均值也随之确定。

在以下常规条件下：

- 使用确定性的平局处理规则；
- 对空簇进行合理处理；
- 每次真正改变分组时目标函数严格下降；

K-means 不可能无限访问新的分组，因此最终会到达稳定状态。

> [!important]
> K-means 的“收敛”并不代表找到了全局最优解。
>
> 它通常只能保证到达一个局部最优点或稳定点。不同初始化可能得到不同结果。

---

## 八、具体计算示例

给定四个二维点：

$$
A=(1,1)
$$

$$
B=(1,2)
$$

$$
C=(4,4)
$$

$$
D=(5,4)
$$

希望将其划分成两个簇：

$$
K=2
$$

---

### 初始化中心

假设选择：

$$
\mu_1^{(0)}=(1,1)
$$

$$
\mu_2^{(0)}=(5,4)
$$

---

### 第一次分配样本

#### 样本 $A=(1,1)$

到第一个中心：

$$
\|A-\mu_1\|_2^2
=
(1-1)^2+(1-1)^2
=
0
$$

到第二个中心：

$$
\|A-\mu_2\|_2^2
=
(1-5)^2+(1-4)^2
=
16+9
=
25
$$

因此：

$$
A\in C_1
$$

---

#### 样本 $B=(1,2)$

到第一个中心：

$$
\|B-\mu_1\|_2^2
=
(1-1)^2+(2-1)^2
=
1
$$

到第二个中心：

$$
\|B-\mu_2\|_2^2
=
(1-5)^2+(2-4)^2
=
16+4
=
20
$$

因此：

$$
B\in C_1
$$

---

#### 样本 $C=(4,4)$

到第一个中心：

$$
\|C-\mu_1\|_2^2
=
(4-1)^2+(4-1)^2
=
9+9
=
18
$$

到第二个中心：

$$
\|C-\mu_2\|_2^2
=
(4-5)^2+(4-4)^2
=
1
$$

因此：

$$
C\in C_2
$$

---

#### 样本 $D=(5,4)$

到第一个中心：

$$
\|D-\mu_1\|_2^2
=
(5-1)^2+(4-1)^2
=
16+9
=
25
$$

到第二个中心：

$$
\|D-\mu_2\|_2^2
=
(5-5)^2+(4-4)^2
=
0
$$

因此：

$$
D\in C_2
$$

---

### 第一次分组结果

$$
C_1=\{A,B\}
$$

$$
C_2=\{C,D\}
$$

| 样本 | 到 $\mu_1$ 的平方距离 | 到 $\mu_2$ 的平方距离 | 所属簇 |
|---|---:|---:|---|
| $A=(1,1)$ | $0$ | $25$ | $C_1$ |
| $B=(1,2)$ | $1$ | $20$ | $C_1$ |
| $C=(4,4)$ | $18$ | $1$ | $C_2$ |
| $D=(5,4)$ | $25$ | $0$ | $C_2$ |

---

### 更新第一个中心

第一个簇包含：

$$
(1,1),(1,2)
$$

新的横坐标为：

$$
\frac{1+1}{2}=1
$$

新的纵坐标为：

$$
\frac{1+2}{2}=1.5
$$

所以：

$$
\mu_1^{(1)}=(1,1.5)
$$

---

### 更新第二个中心

第二个簇包含：

$$
(4,4),(5,4)
$$

新的横坐标为：

$$
\frac{4+5}{2}=4.5
$$

新的纵坐标为：

$$
\frac{4+4}{2}=4
$$

所以：

$$
\mu_2^{(1)}=(4.5,4)
$$

---

### 第二次分配样本

使用新中心：

$$
\mu_1=(1,1.5)
$$

$$
\mu_2=(4.5,4)
$$

重新计算后：

- $A$ 仍然属于 $C_1$；
- $B$ 仍然属于 $C_1$；
- $C$ 仍然属于 $C_2$；
- $D$ 仍然属于 $C_2$。

分组不再改变，中心也不再改变，因此算法停止。

最终结果为：

$$
C_1=\{(1,1),(1,2)\}
$$

$$
\mu_1=(1,1.5)
$$

以及：

$$
C_2=\{(4,4),(5,4)\}
$$

$$
\mu_2=(4.5,4)
$$

---

### 最终目标函数

第一个簇的平方距离和：

$$
\|(1,1)-(1,1.5)\|_2^2
=
0.25
$$

$$
\|(1,2)-(1,1.5)\|_2^2
=
0.25
$$

因此：

$$
J_1=0.5
$$

第二个簇的平方距离和：

$$
\|(4,4)-(4.5,4)\|_2^2
=
0.25
$$

$$
\|(5,4)-(4.5,4)\|_2^2
=
0.25
$$

因此：

$$
J_2=0.5
$$

总目标函数为：

$$
J=J_1+J_2=1
$$

---

## 九、K-means 的伪代码

```text
输入：
    样本集合 X
    聚类数量 K

初始化：
    选择 K 个初始聚类中心

重复执行：
    1. 对于每个样本：
       计算它到所有中心的平方欧氏距离
       将它分配给距离最近的中心

    2. 对于每个簇：
       计算簇内所有样本的均值
       使用均值更新聚类中心

直到：
    分组不再改变
    或中心移动很小
    或达到最大迭代次数

输出：
    每个样本的簇标签
    每个簇的聚类中心
```

---

## 十、Python 手动实现 K-means

下面给出一个简化的 K-means 实现。

```python
from __future__ import annotations

import numpy as np
from numpy.typing import NDArray


def squared_euclidean_distances(
    samples: NDArray[np.float64],
    centers: NDArray[np.float64],
) -> NDArray[np.float64]:
    """
    计算每个样本到每个聚类中心的平方欧氏距离。

    samples:
        形状为 [n_samples, n_features]

    centers:
        形状为 [k, n_features]

    返回:
        形状为 [n_samples, k]
    """
    differences = samples[:, None, :] - centers[None, :, :]
    return np.sum(differences**2, axis=2)


def kmeans(
    samples: NDArray[np.float64],
    k: int,
    max_iterations: int = 100,
    tolerance: float = 1e-6,
    random_seed: int = 0,
) -> tuple[NDArray[np.int64], NDArray[np.float64]]:
    """
    使用平方欧氏距离实现简化版 K-means。
    """
    if samples.ndim != 2:
        raise ValueError("samples 必须是二维数组")

    n_samples = samples.shape[0]

    if not 1 <= k <= n_samples:
        raise ValueError("k 必须位于 1 到样本数量之间")

    random_generator = np.random.default_rng(random_seed)

    initial_indices = random_generator.choice(
        n_samples,
        size=k,
        replace=False,
    )
    centers = samples[initial_indices].copy()

    labels = np.zeros(n_samples, dtype=np.int64)

    for _ in range(max_iterations):
        distances = squared_euclidean_distances(
            samples,
            centers,
        )

        new_labels = np.argmin(distances, axis=1)
        new_centers = centers.copy()

        for cluster_id in range(k):
            cluster_samples = samples[new_labels == cluster_id]

            if len(cluster_samples) == 0:
                # 简单处理：空簇暂时保留原中心
                continue

            new_centers[cluster_id] = np.mean(
                cluster_samples,
                axis=0,
            )

        center_shift = np.linalg.norm(
            new_centers - centers
        )

        centers = new_centers
        labels = new_labels

        if center_shift < tolerance:
            break

    return labels, centers


def main() -> None:
    samples = np.array(
        [
            [1.0, 1.0],
            [1.0, 2.0],
            [4.0, 4.0],
            [5.0, 4.0],
        ],
        dtype=np.float64,
    )

    labels, centers = kmeans(
        samples=samples,
        k=2,
        random_seed=1,
    )

    print("簇标签：")
    print(labels)

    print("聚类中心：")
    print(centers)


if __name__ == "__main__":
    main()
```

---

## 十一、使用 scikit-learn 实现

实际工程中可以直接使用 `scikit-learn`。

```python
from __future__ import annotations

import numpy as np
from sklearn.cluster import KMeans
from sklearn.preprocessing import StandardScaler


def main() -> None:
    samples = np.array(
        [
            [1.0, 1.0],
            [1.0, 2.0],
            [4.0, 4.0],
            [5.0, 4.0],
        ],
        dtype=np.float64,
    )

    scaler = StandardScaler()
    normalized_samples = scaler.fit_transform(samples)

    model = KMeans(
        n_clusters=2,
        init="k-means++",
        n_init=10,
        max_iter=300,
        random_state=0,
    )

    labels = model.fit_predict(normalized_samples)

    normalized_centers = model.cluster_centers_
    original_centers = scaler.inverse_transform(
        normalized_centers
    )

    print("簇标签：")
    print(labels)

    print("原始坐标空间中的聚类中心：")
    print(original_centers)

    print("簇内平方距离和：")
    print(model.inertia_)


if __name__ == "__main__":
    main()
```

其中：

- `n_clusters=2`：设置簇数量 $K=2$；
- `init="k-means++"`：使用 K-means++ 初始化；
- `n_init=10`：使用不同初始中心运行多次；
- `max_iter=300`：每次运行最多迭代300轮；
- `random_state=0`：固定随机结果；
- `cluster_centers_`：最终聚类中心；
- `labels_`：每个样本所属簇；
- `inertia_`：最终簇内平方距离和。

---

## 十二、为什么要进行特征标准化

假设样本具有两个特征：

$$
x=(\text{身高},\text{收入})
$$

某两个样本之间的差异为：

$$
\Delta\text{身高}=10
$$

$$
\Delta\text{收入}=5000
$$

平方欧氏距离为：

$$
d^2
=
10^2+5000^2
$$

收入特征会完全主导距离。

即使身高对任务同样重要，它对距离的贡献也几乎可以忽略。

因此，使用 K-means 前通常需要进行[[特征标准化]]：

$$
x'
=
\frac{x-\mu}{\sigma}
$$

其中：

- $\mu$：该特征的均值；
- $\sigma$：该特征的标准差。

标准化后，各特征通常具有：

$$
\text{均值}\approx0
$$

$$
\text{标准差}\approx1
$$

> [!warning]
> 使用欧氏距离的算法对特征尺度非常敏感。
>
> 如果不同特征的量纲和数量级差异明显，却不进行标准化，聚类结果可能主要反映数值最大的特征，而不是实际最重要的特征。

相关笔记：

- [[数据标准化]]
- [[数据归一化]]
- [[特征工程]]
- [[欧氏距离]]

---

## 十三、初始化问题

### 随机初始化的缺点

K-means 的目标函数不是一个容易求全局最优解的凸优化问题。

不同初始中心可能导致不同结果。

例如，同一批数据可能得到：

$$
J=100
$$

也可能得到：

$$
J=70
$$

两种结果都可能已经收敛，但第二种结果的簇内平方距离更小。

---

### K-means++ 初始化

K-means++ 的基本思想是：

> 已经选中的中心附近不急于再选新中心，而优先从距离现有中心较远的样本中选择下一个中心。

其过程可以概括为：

1. 随机选择第一个中心；
2. 计算每个样本到最近已有中心的平方距离；
3. 距离越远的样本，被选为新中心的概率越大；
4. 重复直到选出 $K$ 个中心。

这样可以使初始中心更加分散，通常能：

- 减少较差初始化；
- 加快收敛；
- 提高聚类稳定性；
- 获得更小的最终目标函数。

---

### 多次运行

实践中通常采用：

```text
不同初始中心运行多次
→ 比较最终目标函数
→ 选择目标函数最小的一次
```

这也是 `scikit-learn` 中 `n_init` 参数的作用。

---

## 十四、如何选择 K

K-means 需要提前指定簇数量：

$$
K
$$

但实际问题中，合适的 $K$ 往往未知。

---

### 肘部法

对不同的 $K$ 运行 K-means，记录簇内平方距离和：

$$
J(K)
$$

随着 $K$ 增大，目标函数通常会下降。

这是因为簇数量越多，每个样本越容易靠近某个中心。

当 $K$ 增加到某个位置后，继续增加簇数量带来的收益明显减小，曲线可能出现类似手肘的拐点。

该位置可以作为候选 $K$。

> [!warning]
> 目标函数会随着 $K$ 增大而单调不增。
>
> 因此不能简单选择使 $J$ 最小的 $K$，否则极端情况下 $K=n$ 时，每个样本单独成为一个簇，可以得到 $J=0$，但这种划分通常没有实际意义。

---

### 轮廓系数

[[轮廓系数]]同时考虑：

- 样本与本簇样本的接近程度；
- 样本与其他簇的分离程度。

对于一个样本：

$$
s
=
\frac{b-a}{\max(a,b)}
$$

其中：

- $a$：样本与本簇其他样本的平均距离；
- $b$：样本与最近其他簇样本的平均距离。

轮廓系数越接近 $1$，通常说明聚类越合理。

---

### 业务或先验知识

在很多任务中，$K$ 可以由问题本身决定。

例如：

- 用户需要分成3类运营人群；
- 图像颜色需要量化成8种代表颜色；
- Anchor需要设置成9组；
- 已知数据中存在4种典型行为模式。

此时可以直接依据任务要求选择 $K$。

---

## 十五、空簇问题

某次分配后，可能出现某个簇没有任何样本：

$$
C_k=\varnothing
$$

此时无法计算：

$$
\mu_k
=
\frac{1}{|C_k|}
\sum_{x_i\in C_k}x_i
$$

因为：

$$
|C_k|=0
$$

常见处理方法包括：

- 保留原中心；
- 随机选择一个样本作为新中心；
- 选择距离当前所有中心最远的样本；
- 拆分样本数量较多的簇；
- 重新初始化该中心。

> [!note]
> 如果空簇处理方式会随机改变中心，目标函数可能暂时增大，因此标准的单调下降证明需要结合具体实现分析。

---

## 十六、K-means 的优点

K-means 的主要优点包括：

- 思想简单；
- 容易实现；
- 计算速度较快；
- 适合较大数据集；
- 聚类中心容易解释；
- 可以方便地预测新样本所属簇；
- 在球状、紧凑、尺度相近的簇上效果较好。

其每轮迭代的主要计算复杂度约为：

$$
O(nKd)
$$

其中：

- $n$：样本数量；
- $K$：簇数量；
- $d$：特征维度。

如果迭代 $T$ 轮，总复杂度约为：

$$
O(nKdT)
$$

---

## 十七、K-means 的局限性

### 必须提前指定 K

K-means 不会自动决定簇数量。

---

### 对初始中心敏感

不同初始化可能得到不同的局部最优结果。

---

### 对异常值敏感

均值容易被极端样本拉动。

例如一个簇中大部分点位于：

$$
1,2,3
$$

但加入异常值：

$$
100
$$

均值会明显改变：

$$
\frac{1+2+3+100}{4}=26.5
$$

因此聚类中心可能被异常值拖离主要样本区域。

---

### 偏好球状或凸状簇

K-means 依据“距离哪个中心最近”划分空间。

其决策边界本质上形成 Voronoi 区域，因此更适合：

- 近似球状；
- 各方向尺度相近；
- 簇密度相近；
- 簇大小相近的数据。

对于月牙形、环形或复杂非凸簇，K-means 可能无法正确划分。

相关算法：

- [[DBSCAN]]
- [[谱聚类]]
- [[层次聚类]]

---

### 对特征尺度敏感

不同特征的量纲和数量级会直接影响欧氏距离。

---

### 不适合普通类别型特征

例如：

```text
颜色 = 红、绿、蓝
```

这些类别之间通常没有自然的欧氏距离。

若直接编码为：

```text
红 = 0
绿 = 1
蓝 = 2
```

会人为引入：

$$
d(\text{红},\text{蓝})
>
d(\text{红},\text{绿})
$$

这种关系未必合理。

---

## 十八、常见误区

### 误区一：聚类算法中一定使用欧氏距离

错误。

不同聚类算法可以使用不同距离或相似度，例如：

- 欧氏距离；
- 曼哈顿距离；
- 余弦距离；
- 马氏距离；
- IoU距离；
- 自定义距离。

标准 K-means 与平方欧氏距离紧密相关，但其他聚类算法不一定使用欧氏距离。

---

### 误区二：平方欧氏距离不是距离，所以不能使用

平方欧氏距离通常不满足严格距离度量中的三角不等式，因此从数学定义上不一定属于标准“度量”。

但 K-means 的目标函数完全可以使用平方欧氏距离。

它具有：

- 非负性；
- 同一点距离为零；
- 便于优化；
- 均值为最优中心；

因此非常适合 K-means。

---

### 误区三：平方欧氏距离和欧氏距离会产生不同的最近中心

对于同一组中心，它们不会产生不同的最近中心。

因为平方根是单调函数：

$$
\arg\min_k
\|x-\mu_k\|_2
=
\arg\min_k
\|x-\mu_k\|_2^2
$$

但在整体优化目标上，平方距离会更强烈地惩罚远距离样本。

---

### 误区四：K-means 收敛就说明找到了全局最优解

错误。

K-means 通常只保证：

- 目标函数不再明显下降；
- 样本分组稳定；
- 聚类中心稳定；
- 到达局部最优或稳定点。

不同初始化可能得到不同结果。

---

### 误区五：簇编号具有固定意义

错误。

一次运行中：

```text
簇0
```

可能代表左侧样本；

另一次运行中：

```text
簇1
```

可能代表同一组样本。

簇编号只是标签，编号交换不会改变聚类本质。

---

### 误区六：目标函数越小，聚类一定越合理

不完全正确。

在相同 $K$ 下，更小的目标函数通常表示簇内更紧凑。

但不同 $K$ 之间不能只比较目标函数，因为增加 $K$ 几乎总能降低目标函数。

同时，目标函数较小也不一定符合业务语义。

---

### 误区七：聚类中心一定是某个真实样本

错误。

K-means 中心是样本均值，因此可能不等于任何真实样本。

例如：

$$
(1,1)
$$

和：

$$
(1,2)
$$

的中心为：

$$
(1,1.5)
$$

它不一定是原始数据中的实际样本。

如果要求中心必须是实际样本，可以考虑 [[K-medoids]]。

---

### 误区八：不需要对数据进行标准化

错误。

只要不同特征量纲明显不同，欧氏距离就可能被某个大尺度特征主导。

---

### 误区九：K-means 可以处理任意形状的簇

错误。

K-means 更适合接近球状、尺度相近、密度相近的簇。

---

## 十九、K-means 与其他聚类算法对比

| 算法 | 是否需要指定簇数 | 主要特点 | 适用情况 |
|---|---|---|---|
| K-means | 需要 | 使用均值和平方欧氏距离 | 球状、紧凑的数值数据 |
| K-medoids | 需要 | 中心必须是真实样本 | 对异常值更稳健 |
| 层次聚类 | 可选 | 构建树状聚类结构 | 希望观察多层次关系 |
| DBSCAN | 不直接需要 | 基于密度，可识别噪声 | 非凸簇、含异常点 |
| 谱聚类 | 需要 | 基于相似图结构 | 复杂形状的簇 |
| 高斯混合模型 | 需要 | 概率式软聚类 | 椭圆形簇、需要概率 |

---

## 二十、K-means 与 Anchor 聚类

在经典 Anchor-based 目标检测算法中，可以将每个真实框表示为：

$$
x_i=(w_i,h_i)
$$

然后使用聚类寻找代表性宽高。

但如果直接使用普通欧氏距离：

$$
d^2
=
(w_i-w_j)^2+(h_i-h_j)^2
$$

大框的绝对尺寸差异可能更容易主导聚类。

因此，部分目标检测实现会使用基于 IoU 的距离：

$$
d_{\text{IoU}}
=
1-IoU(\text{box},\text{centroid})
$$

这类做法虽然常被称为“K-means Anchor聚类”，但它与最标准的平方欧氏距离 K-means 并不完全相同。

特别是当距离被替换后：

- 中心更新方式是否仍取算术均值；
- 目标函数是否仍然单调下降；
- 是否还能使用标准K-means收敛证明；

都需要重新分析。

> [!important]
> “借用了 K-means 的迭代框架”不代表它仍然是严格意义上的标准 K-means。
>
> 标准 K-means 的数学性质建立在平方欧氏距离和均值更新之上。

相关笔记：

- [[Anchor]]
- [[YOLO]]
- [[IoU]]
- [[目标检测]]
- [[边界框回归]]

---

## 核心总结

聚类属于[[无监督学习]]，它根据样本之间的相似性自动发现数据中的分组结构。

标准 K-means 的目标函数为：

$$
\boxed{
J
=
\sum_{k=1}^{K}
\sum_{x_i\in C_k}
\|x_i-\mu_k\|_2^2
}
$$

算法不断交替执行两个步骤：

$$
\boxed{
\text{固定中心，将样本分给最近中心}
}
$$

$$
\boxed{
\text{固定分组，将中心更新为簇内均值}
}
$$

之所以使用均值，是因为在平方欧氏距离下，均值能够最小化簇内平方距离之和：

$$
\boxed{
\mu_k
=
\frac{1}{|C_k|}
\sum_{x_i\in C_k}x_i
}
$$

每次分配和中心更新都不会使目标函数增大，因此：

$$
J^{(t+1)}
\leq
J^{(t)}
$$

在有限数据、确定性平局规则和合理空簇处理下，K-means 会达到稳定状态。

但 K-means 仍然存在以下限制：

- 需要提前指定 $K$；
- 对初始化敏感；
- 只能保证局部最优；
- 对异常值敏感；
- 对特征尺度敏感；
- 更适合球状、紧凑、尺度相近的簇；
- 不适合直接处理复杂非凸结构和普通类别特征。

完整流程可以概括为：

$$
\boxed{
\text{选择 }K
\rightarrow
\text{初始化中心}
\rightarrow
\text{计算平方距离}
\rightarrow
\text{分配样本}
\rightarrow
\text{更新均值中心}
\rightarrow
\text{迭代至稳定}
}
$$

---

## 相关笔记

- [[机器学习]]
- [[无监督学习]]
- [[监督学习]]
- [[聚类算法]]
- [[欧氏距离]]
- [[平方欧氏距离]]
- [[特征标准化]]
- [[数据归一化]]
- [[梯度]]
- [[局部最优]]
- [[K-means++]]
- [[轮廓系数]]
- [[肘部法]]
- [[K-medoids]]
- [[层次聚类]]
- [[DBSCAN]]
- [[谱聚类]]
- [[高斯混合模型]]
- [[Anchor]]
- [[IoU]]
