#t计算机视觉-深度学习-目标检测-yolo 
---
title: YOLO 目标检测模型完整训练过程
aliases:
  - YOLO训练流程
  - YOLO目标检测训练
tags:
  - 计算机视觉
  - 深度学习
  - 目标检测
  - YOLO
  - 模型训练
status: learning
---

## YOLO 目标检测模型完整训练过程

## 摘要

YOLO 目标检测模型的训练，本质上是让模型不断完成以下闭环：

$$
\text{输入图像}
\rightarrow
\text{前向传播}
\rightarrow
\text{产生预测}
\rightarrow
\text{与真实标签比较}
\rightarrow
\text{计算损失}
\rightarrow
\text{反向传播}
\rightarrow
\text{更新参数}
$$

训练开始时，模型还不能准确识别目标，预测框的位置和类别置信度可能都不正确。

训练程序会利用人工标注的真实框计算预测误差，再通过反向传播计算每个模型参数的梯度，最后由优化器修改模型参数。经过大量迭代后，模型逐渐学会：

- 从图像中提取目标特征；
- 判断图像中是否存在目标；
- 预测目标所属类别；
- 预测目标检测框的位置；
- 为预测结果给出置信度。

对于蜜蜂检测任务，完整系统通常可以表示为：

$$
\text{视频}
\rightarrow
\text{YOLO逐帧检测}
\rightarrow
\text{目标跟踪}
\rightarrow
\text{轨迹分析}
\rightarrow
\text{蜜蜂行为分析}
$$

其中，YOLO主要解决的问题是：

> 图像中是否存在蜜蜂，以及蜜蜂位于什么位置。

---

## 核心概念

### 模型

模型可以看作一个带有大量可学习参数的函数：

$$
\hat{y}=f_{\theta}(x)
$$

其中：

- $x$：输入图像；
- $\theta$：模型中的全部可学习参数；
- $f_{\theta}$：YOLO神经网络；
- $\hat{y}$：模型输出的预测结果。

模型中的参数包括：

- 卷积核权重；
- 卷积层偏置；
- BatchNorm中的可学习缩放和偏移参数；
- 检测头中的分类和回归参数。

训练的目标是找到一组更合适的参数 $\theta^*$：

$$
\theta^*
=
\arg\min_{\theta}
L\left(f_{\theta}(x),y\right)
$$

其中：

- $y$：人工标注的真实标签；
- $L$：损失函数；
- $\theta^*$：训练后得到的较优参数。

---

### 真实标签

真实标签也称为 Ground Truth。

它由人工标注产生，用于告诉模型：

- 图像中有哪些目标；
- 每个目标属于什么类别；
- 每个目标的检测框在哪里。

在YOLO格式中，一行标签通常表示一个目标：

```text
class_id x_center y_center width height
```

例如：

```text
0 0.35 0.42 0.10 0.08
```

表示：

- 类别编号为 $0$；
- 检测框中心横坐标为 $0.35$；
- 检测框中心纵坐标为 $0.42$；
- 检测框宽度为 $0.10$；
- 检测框高度为 $0.08$。

这些坐标通常已经除以图像宽高，归一化到 $0$ 到 $1$ 之间。

---

### 前向传播

前向传播是指输入图像按照网络结构逐层计算，最终产生预测结果的过程：

$$
x
\rightarrow
\text{Backbone}
\rightarrow
\text{Neck}
\rightarrow
\text{Detection Head}
\rightarrow
\hat{y}
$$

前向传播只负责根据当前参数产生预测，并不会修改模型参数。

---

### 损失函数

损失函数用于衡量：

> 模型预测结果和人工标注结果之间相差多少。

损失越大，表示预测越差；损失越小，表示预测越接近真实标签。

YOLO目标检测训练中通常包括：

- 检测框回归损失；
- 类别损失；
- DFL边界分布损失。

总损失可以概括为：

$$
L_{\text{total}}
=
\lambda_{\text{box}}L_{\text{box}}
+
\lambda_{\text{cls}}L_{\text{cls}}
+
\lambda_{\text{dfl}}L_{\text{dfl}}
$$

---

### 反向传播

反向传播根据损失函数，计算每个模型参数对损失的影响：

$$
\frac{\partial L}{\partial \theta}
$$

这些偏导数组成模型参数的梯度。

梯度告诉优化器：

- 哪些参数需要增大；
- 哪些参数需要减小；
- 每个参数的调整方向和调整程度。

---

### 优化器

优化器根据梯度修改模型参数。

最基础的梯度下降更新公式为：

$$
\theta_{\text{new}}
=
\theta_{\text{old}}
-
\eta
\frac{\partial L}{\partial \theta}
$$

其中：

- $\eta$：学习率；
- $\frac{\partial L}{\partial \theta}$：损失关于参数的梯度。

---

### Batch与Epoch

Batch表示一次送入模型的样本数量。

例如：

```python
batch = 8
```

表示模型一次处理8张图片，然后更新一次参数。

Epoch表示模型完整学习一次训练集。

假设训练集有1600张图片，批量大小为8，那么一个Epoch大约包含：

$$
\frac{1600}{8}=200
$$

次参数更新。

---

## 一、准备目标检测数据集

### 图片和标签必须一一对应

一个典型YOLO数据集可以组织为：

```text
bee_dataset/
├── images/
│   ├── train/
│   ├── val/
│   └── test/
├── labels/
│   ├── train/
│   ├── val/
│   └── test/
└── bee.yaml
```

例如：

```text
images/train/001.jpg
labels/train/001.txt
```

图片文件和标签文件通常具有相同的主文件名。

---

### 训练集、验证集和测试集

#### 训练集

训练集用于：

- 前向传播；
- 计算损失；
- 反向传播；
- 更新模型参数。

因此，训练集会直接影响模型学到的参数。

#### 验证集

验证集用于：

- 检查模型泛化能力；
- 计算Precision；
- 计算Recall；
- 计算mAP；
- 选择最佳模型；
- 判断是否出现过拟合。

验证集不会参与参数更新。

#### 测试集

测试集通常在模型结构、超参数和训练方案都确定后使用，用于最终评价模型性能。

> [!warning]
> 对视频数据进行划分时，不建议将同一段视频的相邻帧随机分配到训练集和验证集。

例如，第100帧属于训练集，第101帧属于验证集。由于两帧内容非常相似，可能造成数据泄漏，使验证指标虚高。

更合理的方法是按照视频、拍摄时间或场景划分：

```text
视频1、视频2、视频3 → 训练集
视频4 → 验证集
视频5 → 测试集
```

---

### YOLO标签坐标

假设原始图片宽高为：

$$
W=1000,\qquad H=800
$$

一个真实框的左上角和右下角为：

$$
(x_1,y_1)=(200,240)
$$

$$
(x_2,y_2)=(400,400)
$$

检测框的像素宽高为：

$$
w=x_2-x_1=200
$$

$$
h=y_2-y_1=160
$$

检测框中心为：

$$
x_c=\frac{x_1+x_2}{2}=300
$$

$$
y_c=\frac{y_1+y_2}{2}=320
$$

归一化后：

$$
x_c'=\frac{300}{1000}=0.3
$$

$$
y_c'=\frac{320}{800}=0.4
$$

$$
w'=\frac{200}{1000}=0.2
$$

$$
h'=\frac{160}{800}=0.2
$$

对应YOLO标签为：

```text
0 0.3 0.4 0.2 0.2
```

其中类别编号 $0$ 可以代表 `bee`。

---

### 数据集配置文件

一个简单的 `bee.yaml` 可以写为：

```yaml
path: /root/bee_dataset

train: images/train
val: images/val
test: images/test

names:
  0: bee
```

其中：

- `path`：数据集根目录；
- `train`：训练集图片路径；
- `val`：验证集图片路径；
- `test`：测试集图片路径；
- `names`：类别编号与类别名称的对应关系。

---

## 二、选择模型和初始化参数

### 使用预训练模型

使用Ultralytics训练YOLO时，可以先加载预训练权重：

```python
from ultralytics import YOLO

model = YOLO("yolo11m.pt")
```

`yolo11m.pt` 中已经保存了一组预训练参数。

这些参数可能已经从大型数据集中学习到：

- 边缘；
- 纹理；
- 颜色变化；
- 局部形状；
- 常见目标结构；
- 通用视觉语义。

虽然预训练模型不一定专门学习过巢门口蜜蜂，但它学到的基础视觉能力仍然可以迁移到蜜蜂检测任务中。

这叫作：

$$
\text{迁移学习}
$$

迁移学习过程可以表示为：

$$
\text{通用视觉特征}
\rightarrow
\text{蜜蜂检测特征}
$$

---

### 从头训练

也可以只加载网络结构：

```python
from ultralytics import YOLO

model = YOLO("yolo11m.yaml")
```

这种方式通常会随机初始化大部分模型参数。

从头训练一般需要：

- 更多训练数据；
- 更多训练轮数；
- 更强计算资源；
- 更谨慎的学习率设置；
- 更充分的数据增强。

对于数据量有限的自定义检测任务，通常优先使用预训练权重。

---

## 三、YOLO模型内部结构

YOLO检测模型可以大致分成三个部分：

```text
输入图像
   ↓
Backbone
   ↓
Neck
   ↓
Detection Head
   ↓
检测结果
```

---

### Backbone：主干特征提取网络

Backbone负责从输入图像中提取特征。

输入图像张量可能为：

$$
[3,640,640]
$$

其中：

- $3$：RGB三个通道；
- $640$：图像高度；
- $640$：图像宽度。

经过多层卷积后，模型逐渐提取不同层次的特征。

浅层特征通常包括：

- 边缘；
- 轮廓；
- 纹理；
- 颜色变化；
- 蜜蜂翅膀边界。

深层特征通常包括：

- 蜜蜂身体结构；
- 蜜蜂整体形状；
- 目标与背景的区别；
- 更抽象的语义信息。

特征提取过程可以概括为：

$$
\text{原始像素}
\rightarrow
\text{边缘和纹理}
\rightarrow
\text{局部结构}
\rightarrow
\text{目标语义}
$$

---

### Neck：多尺度特征融合

在巢外蜜蜂检测中，蜜蜂可能具有不同尺寸：

- 近处蜜蜂较大；
- 远处蜜蜂较小；
- 飞行中的蜜蜂可能模糊；
- 重叠蜜蜂可能只露出部分身体。

深层特征图包含较强的语义信息，但分辨率较低。

浅层特征图分辨率较高，但语义信息相对较弱。

Neck负责融合不同层级的特征，使模型同时利用：

- 高分辨率局部信息；
- 低分辨率语义信息；
- 不同尺度的目标信息。

可以简单理解为：

```text
高分辨率特征 → 更适合小目标
中分辨率特征 → 更适合中等目标
低分辨率特征 → 更适合大目标
```

---

### Detection Head：检测头

检测头根据融合后的特征产生目标检测结果。

它主要学习：

- 某个位置是否存在目标；
- 目标属于哪个类别；
- 目标检测框位于哪里。

对于单类别蜜蜂检测，检测头主要输出：

```text
类别：bee
检测框位置
类别置信度
```

检测框可以表示为：

$$
(x,y,w,h)
$$

其中：

- $x$：检测框中心横坐标；
- $y$：检测框中心纵坐标；
- $w$：检测框宽度；
- $h$：检测框高度。

模型内部也可能采用目标点到检测框四条边界距离的形式进行回归。

---

## 四、读取一个Batch

训练时通常不会一次将整个训练集送入GPU，而是将数据分成多个Batch。

假设设置：

```python
batch = 8
```

一次输入模型的图像张量可能为：

$$
[8,3,640,640]
$$

各个维度含义为：

```text
8   → 图片数量
3   → RGB通道数
640 → 图像高度
640 → 图像宽度
```

一个Batch的完整训练过程是：

```text
读取一批图片和标签
→ 图像预处理
→ 数据增强
→ 前向传播
→ 计算损失
→ 反向传播
→ 更新一次参数
```

---

## 五、图像预处理

### 调整输入尺寸

训练图片的原始大小可能不同，但同一个Batch中的张量必须具有统一尺寸。

例如设置：

```python
imgsz = 1024
```

模型会将图片调整到适合训练的大小。

通常会尽量保持原图长宽比，并在多余区域进行填充，这种操作常称为Letterbox。

调整图片后，检测框坐标也必须同步变化。

---

### 像素归一化

原始RGB像素值通常位于：

$$
0\sim255
$$

进入模型前，可以缩放为：

$$
x'=\frac{x}{255}
$$

得到：

$$
0\sim1
$$

这样可以让输入数据处于较稳定的数值范围。

---

## 六、数据增强

数据增强是指在训练期间随机改变图像和标签，产生更多不同形式的训练样本。

常见增强包括：

- 水平翻转；
- 垂直翻转；
- 缩放；
- 平移；
- 旋转；
- 颜色和亮度变化；
- 随机裁剪；
- Mosaic；
- MixUp。

例如原图中的蜜蜂位于左侧，水平翻转后蜜蜂位于右侧。

图片翻转后，标签框也必须同步翻转：

$$
\text{图像变换}
\Longrightarrow
\text{标签坐标同步变换}
$$

数据增强的作用是让模型学习：

> 即使目标位置、大小、方向、背景和光照发生变化，目标仍然属于同一个类别。

在蜜蜂检测任务中，数据增强可以帮助模型适应：

- 早晚光照变化；
- 阴影和反光；
- 蜜蜂飞行方向变化；
- 摄像机距离变化；
- 蜜蜂部分遮挡；
- 蜂箱背景变化。

> [!warning]
> 数据增强并不是越强越好。
>
> 如果目标本身非常小，过强裁剪、缩放或旋转可能使目标信息丢失，甚至造成标签和图像不匹配。

---

## 七、前向传播

处理好的图片被送入YOLO模型：

$$
\hat{y}=f_{\theta}(x)
$$

完整过程为：

```text
输入图片
→ 卷积和特征提取
→ 多尺度特征融合
→ 检测头
→ 候选检测结果
```

训练开始时，模型的预测通常比较差，可能出现：

- 预测框偏离真实目标；
- 预测框过大或过小；
- 真实蜜蜂置信度很低；
- 背景被误判为蜜蜂；
- 一个目标产生多个候选框；
- 部分蜜蜂没有有效预测。

这些预测会在后续与真实标签进行比较。

---

## 八、标签分配与正负样本匹配

模型可能在一张图片上产生大量候选预测位置，但真实目标数量通常较少。

例如：

```text
候选预测位置：数千个
真实蜜蜂数量：5只
```

训练程序必须确定：

- 哪些候选预测负责学习第1只蜜蜂；
- 哪些候选预测负责学习第2只蜜蜂；
- 哪些候选预测属于正样本；
- 哪些候选位置应该被视为背景。

这个过程称为：

$$
\text{标签分配}
$$

或：

$$
\text{正负样本匹配}
$$

可以理解为：

```text
真实检测框
   ↓
寻找与真实框位置和类别较匹配的候选预测
   ↓
将合适的候选预测设为正样本
   ↓
其他候选位置主要作为背景样本
```

正样本通常负责学习：

- 类别信息；
- 检测框位置；
- 检测框边界分布。

> [!note]
> 训练阶段通常不会先进行最终NMS，再利用NMS结果计算损失。
>
> 训练主要基于密集预测和标签分配计算损失。NMS通常出现在传统YOLO模型的验证和推理后处理阶段。

---

## 九、计算损失函数

现代YOLO训练日志中通常会看到：

```text
box_loss
cls_loss
dfl_loss
```

总损失可以写为：

$$
L_{\text{total}}
=
\lambda_{\text{box}}L_{\text{box}}
+
\lambda_{\text{cls}}L_{\text{cls}}
+
\lambda_{\text{dfl}}L_{\text{dfl}}
$$

其中，不同的 $\lambda$ 用于调节各损失项的重要程度。

---

### Box Loss：检测框回归损失

检测框回归损失用于衡量：

> 预测框与真实框的位置和形状相差多少。

假设真实框正确包围蜜蜂，但预测框：

- 向右偏移；
- 宽度过大；
- 高度过小；
- 只覆盖蜜蜂的一部分。

那么检测框损失会增大。

目标检测中常使用IoU及其变体衡量预测框与真实框的重叠程度。

IoU定义为：

$$
IoU
=
\frac{\text{预测框与真实框的交集面积}}
{\text{预测框与真实框的并集面积}}
$$

当预测框和真实框完全重合时：

$$
IoU=1
$$

当两个框完全没有交集时：

$$
IoU=0
$$

训练会推动预测框逐渐靠近真实框。

相关笔记：

- [[IoU]]
- [[检测框回归]]
- [[边界框损失函数]]

---

### Classification Loss：类别损失

类别损失用于衡量模型对目标类别的判断是否正确。

假设真实类别是蜜蜂，但模型预测：

$$
P(\text{bee})=0.15
$$

说明模型对蜜蜂类别缺乏信心，类别损失较大。

如果模型预测：

$$
P(\text{bee})=0.98
$$

类别损失通常较小。

即使数据集只有一个类别，类别损失仍然有意义。

因为模型仍然需要学习区分：

```text
目标位置：存在蜜蜂
背景位置：不存在蜜蜂
```

---

### DFL Loss：边界分布损失

DFL全称为Distribution Focal Loss。

它用于帮助模型更加精细地预测检测框边界。

可以把边界位置理解为一个连续值，例如：

$$
5.7
$$

模型可以学习其位于相邻离散位置的概率分布：

```text
位置5：概率0.3
位置6：概率0.7
```

根据这个概率分布，可以得到更精细的连续边界估计。

DFL主要用于提高：

- 检测框边界定位精度；
- 小目标框的细致程度；
- 高IoU阈值下的检测表现。

---

## 十、反向传播

得到总损失后，需要知道：

> 模型中的每一个参数应该如何改变，才能使损失下降。

对于参数 $\theta$，反向传播计算：

$$
\frac{\partial L}{\partial \theta}
$$

该结果称为梯度。

假设某个参数为 $w$。

如果：

$$
\frac{\partial L}{\partial w}>0
$$

说明适当减小 $w$，可能使损失下降。

如果：

$$
\frac{\partial L}{\partial w}<0
$$

说明适当增大 $w$，可能使损失下降。

在PyTorch中，反向传播通常由下面的代码触发：

```python
loss.backward()
```

梯度从检测头向前传播：

```text
损失
↑
Detection Head
↑
Neck
↑
Backbone
```

因此，模型中的不同部分都会逐渐发生变化：

- 检测头学习输出正确类别和检测框；
- Neck学习融合适合目标检测的多尺度信息；
- Backbone学习提取更适合蜜蜂的视觉特征。

相关笔记：

- [[反向传播]]
- [[链式法则]]
- [[梯度]]
- [[自动求导]]

---

## 十一、优化器更新参数

反向传播只负责计算梯度，并不会自动修改参数。

参数更新由优化器完成。

最基础的梯度下降公式为：

$$
w_{\text{new}}
=
w_{\text{old}}
-
\eta
\frac{\partial L}{\partial w}
$$

例如：

$$
w_{\text{old}}=0.5
$$

$$
\frac{\partial L}{\partial w}=2
$$

$$
\eta=0.01
$$

更新后的权重为：

$$
w_{\text{new}}
=
0.5-0.01\times2
=
0.48
$$

一个Batch的训练逻辑可以简化为：

```python
optimizer.zero_grad()

predictions = model(images)

loss = criterion(predictions, targets)

loss.backward()

optimizer.step()
```

各步骤含义：

```text
optimizer.zero_grad()
→ 清除上一个Batch留下的梯度

model(images)
→ 前向传播，得到预测结果

criterion(...)
→ 计算预测结果与真实标签的损失

loss.backward()
→ 反向传播，计算模型参数的梯度

optimizer.step()
→ 根据梯度更新模型参数
```

---

## 十二、学习率

学习率 $\eta$ 决定每次参数更新的步长。

### 学习率过大

可能导致：

- 损失剧烈振荡；
- 跳过较优参数区域；
- 模型难以稳定收敛；
- 参数数值异常；
- 训练结果不稳定。

可以将其理解为下山时步子太大，不断跨过最低点。

### 学习率过小

可能导致：

- 损失下降非常缓慢；
- 训练需要更多Epoch；
- 在有限训练轮数内未充分收敛；
- 长时间停留在较差区域。

### 学习率调度

实际训练通常不会从头到尾使用完全相同的学习率。

常见过程是：

```text
训练初期：Warmup，逐渐提高学习率
训练中期：使用正常学习率学习
训练后期：逐渐降低学习率进行精细调整
```

相关笔记：

- [[学习率]]
- [[学习率调度器]]
- [[Warmup]]
- [[余弦退火]]

---

## 十三、一个Batch的完整训练过程

一个Batch可以按照下面的顺序理解：

```text
1. DataLoader读取一批图片与标签
2. 对图片和标签执行数据增强
3. 将图片转换成张量并送入GPU
4. YOLO执行前向传播
5. 检测头产生密集候选预测
6. 标签分配器匹配预测与真实目标
7. 计算box_loss、cls_loss和dfl_loss
8. 对总损失执行反向传播
9. 计算所有可学习参数的梯度
10. 优化器更新模型参数
11. 清除梯度
12. 读取下一个Batch
```

核心循环为：

$$
\text{预测}
\rightarrow
\text{计算错误}
\rightarrow
\text{计算梯度}
\rightarrow
\text{更新参数}
$$

---

## 十四、一个Epoch结束后的验证

当训练集中的所有Batch都处理完后，一个Epoch结束。

模型通常会在验证集上进行评估：

```text
验证图片
→ 前向传播
→ 生成候选框
→ 置信度筛选
→ 去除重复检测框
→ 与真实标签匹配
→ 计算评价指标
```

验证阶段不会进行：

- 损失反向传播；
- 参数梯度计算；
- 优化器更新。

验证集只用于客观观察当前模型的泛化性能。

---

## 十五、目标检测评价指标

### TP、FP和FN

#### TP：True Positive

模型检测出一个目标，并且检测结果与真实目标成功匹配。

#### FP：False Positive

模型输出了一个检测框，但它：

- 实际上是背景；
- 类别错误；
- 或与真实框重叠不足。

这属于误检。

#### FN：False Negative

图像中存在真实目标，但模型没有成功检测到。

这属于漏检。

---

### Precision

Precision表示：

> 模型检测出来的目标中，有多少是真正正确的目标。

公式为：

$$
Precision
=
\frac{TP}{TP+FP}
$$

例如模型输出100个蜜蜂框，其中90个正确：

$$
Precision
=
\frac{90}{100}
=
0.9
$$

Precision较低通常意味着误检较多。

---

### Recall

Recall表示：

> 所有真实目标中，有多少被模型成功检测出来。

公式为：

$$
Recall
=
\frac{TP}{TP+FN}
$$

例如图像中共有100只真实蜜蜂，模型成功检测出80只：

$$
Recall
=
\frac{80}{100}
=
0.8
$$

Recall较低通常意味着漏检较多。

---

### AP

AP是Precision-Recall曲线下的面积。

模型可以通过改变置信度阈值，产生不同的Precision和Recall组合。

因此，AP不是只在单个置信度阈值下计算，而是综合不同置信度阈值的表现。

---

### mAP@0.5

mAP@0.5使用IoU阈值 $0.5$ 判断预测框与真实框是否匹配。

即当：

$$
IoU\geq0.5
$$

预测框才有可能被认为是正确匹配。

对于单类别蜜蜂检测，mAP@0.5基本就是蜜蜂类别在IoU阈值 $0.5$ 下的AP。

---

### mAP@0.5:0.95

mAP@0.5:0.95会分别在以下IoU阈值下计算AP：

$$
0.50,0.55,0.60,\ldots,0.95
$$

然后求平均：

$$
mAP@0.5:0.95
=
\frac{1}{10}
\sum_{t=0.50}^{0.95}AP_t
$$

它比mAP@0.5更加严格。

因为当IoU阈值为 $0.95$ 时，预测框必须与真实框高度重合。

通常：

$$
mAP@0.5:0.95
<
mAP@0.5
$$

---

## 十六、训练损失下降不代表模型一定更好

假设训练过程如下：

| Epoch | 训练损失 | 验证集mAP |
|---:|---:|---:|
| 1 | 5.2 | 0.20 |
| 20 | 2.1 | 0.71 |
| 50 | 1.3 | 0.86 |
| 80 | 0.8 | 0.84 |
| 100 | 0.5 | 0.80 |

可以看到：

- 训练损失持续下降；
- 验证集mAP先上升后下降。

这说明模型可能开始过度记忆训练集，而不能很好地处理新数据。

这种现象称为：

$$
\text{过拟合}
$$

因此，不能只观察训练损失，还应同时观察：

- 验证集Precision；
- 验证集Recall；
- 验证集mAP@0.5；
- 验证集mAP@0.5:0.95；
- 训练集与验证集损失趋势。

相关笔记：

- [[过拟合]]
- [[欠拟合]]
- [[泛化能力]]
- [[训练集验证集测试集]]

---

## 十七、best.pt与last.pt

训练目录通常会包含：

```text
runs/detect/train/
├── weights/
│   ├── best.pt
│   └── last.pt
├── results.csv
├── results.png
├── confusion_matrix.png
└── PR_curve.png
```

### last.pt

`last.pt`表示最后一个Epoch结束后的模型参数。

假设训练100轮：

```text
last.pt = 第100个Epoch结束后的模型
```

### best.pt

`best.pt`表示训练期间验证指标表现最好的模型参数。

例如第76轮验证指标最好：

```text
best.pt = 第76轮模型
```

因此，推理、测试和部署时通常优先使用：

```text
best.pt
```

而不是默认最后一轮一定最好。

---

## 十八、YOLO训练代码示例

```python
from ultralytics import YOLO


def main() -> None:
    model = YOLO("yolo11m.pt")

    model.train(
        data="bee.yaml",
        epochs=100,
        imgsz=1024,
        batch=8,
        device=0,
        workers=4,
        pretrained=True,
        amp=True,
        patience=30,
        close_mosaic=10,
        project="runs/bee",
        name="yolo11m_baseline",
    )


if __name__ == "__main__":
    main()
```

---

### 主要参数说明

#### data

```python
data="bee.yaml"
```

指定数据集配置文件。

#### epochs

```python
epochs=100
```

表示训练集被完整学习100次。

#### imgsz

```python
imgsz=1024
```

表示模型训练时使用的输入图像尺寸。

较大的输入尺寸通常有利于保留小目标细节，但会增加：

- GPU显存占用；
- 计算量；
- 训练时间；
- 推理时间。

#### batch

```python
batch=8
```

表示一次输入模型8张图片。

Batch越大，显存占用通常越高。

#### device

```python
device=0
```

表示使用编号为0的GPU。

#### workers

```python
workers=4
```

表示数据加载时使用的工作进程数量。

#### pretrained

```python
pretrained=True
```

表示使用预训练参数进行训练。

#### amp

```python
amp=True
```

表示使用自动混合精度训练。

混合精度通常可以：

- 减少显存占用；
- 加快GPU训练；
- 在大多数情况下保持训练精度。

#### patience

```python
patience=30
```

表示验证指标连续30轮没有改善时，可以提前停止训练。

#### close_mosaic

```python
close_mosaic=10
```

表示训练最后10轮关闭Mosaic增强。

这样可以让模型在训练后期更多接触接近真实分布的完整图像，进行精细调整。

---

## 十九、训练完成后的推理

```python
from ultralytics import YOLO


model = YOLO(
    "runs/bee/yolo11m_baseline/weights/best.pt"
)

results = model.predict(
    source="bee_video.mp4",
    imgsz=1024,
    conf=0.25,
    iou=0.7,
    save=True,
)
```

推理阶段的流程为：

```text
输入图片或视频帧
→ 图像预处理
→ Backbone提取特征
→ Neck融合多尺度特征
→ Detection Head输出候选框
→ 置信度筛选
→ 去除重复检测框
→ 输出最终检测结果
```

推理阶段没有：

- 真实标签；
- 损失函数；
- 反向传播；
- 参数更新。

推理只使用已经训练好的参数产生检测结果。

---

## 二十、NMS去除重复框

模型可能对同一只蜜蜂输出多个高度重叠的检测框。

例如：

```text
检测框A：置信度0.95
检测框B：置信度0.88
检测框C：置信度0.72
```

这些框可能都对应同一只蜜蜂。

NMS的基本思想是：

```text
选择置信度最高的框
→ 删除与它高度重叠的低置信度框
→ 继续处理剩余框
```

NMS中的IoU阈值用于判断两个预测框是否过度重叠。

> [!note]
> 推理参数中的 `iou` 通常指NMS的IoU阈值，不等同于评价mAP时用于判断预测框是否正确的IoU阈值。

相关笔记：

- [[NMS]]
- [[IoU]]
- [[置信度阈值]]

---

## 二十一、YOLO检测与目标跟踪的关系

YOLO主要进行逐帧检测。

例如：

```text
第1帧：检测到5只蜜蜂
第2帧：检测到6只蜜蜂
第3帧：检测到5只蜜蜂
```

YOLO可以输出：

- 每个蜜蜂框的位置；
- 蜜蜂类别；
- 检测置信度。

但它通常不能仅靠单帧检测判断：

> 第1帧中的某只蜜蜂，是否就是第2帧中的同一只蜜蜂。

目标跟踪器负责进行跨帧关联：

```text
上一帧轨迹
+
当前帧YOLO检测框
↓
位置预测与匹配
↓
为同一个目标保持相同ID
```

完整流程为：

$$
\text{视频帧}
\rightarrow
\text{YOLO检测}
\rightarrow
\text{ByteTrack等跟踪器}
\rightarrow
\text{目标ID和轨迹}
$$

可以这样区分：

```text
YOLO：这一帧中目标在哪里？
跟踪器：不同帧中的目标是不是同一个？
行为分析：该目标正在执行什么行为？
```

相关笔记：

- [[ByteTrack]]
- [[卡尔曼滤波]]
- [[匈牙利算法]]
- [[多目标跟踪]]
- [[IDF1]]
- [[MOTA]]

---

## 二十二、用一张图片串联完整训练过程

假设一张图像中有3只蜜蜂。

### 第一步：读取图像和标签

训练程序读取：

```text
图像：001.jpg
标签：001.txt
```

标签中记录了3只蜜蜂的真实类别和真实框。

### 第二步：数据增强

训练程序可能对图像执行：

- 翻转；
- 缩放；
- 平移；
- 颜色变化。

同时同步修改3个真实框的坐标。

### 第三步：前向传播

增强后的图像经过：

```text
Backbone
→ Neck
→ Detection Head
```

模型产生大量候选预测。

### 第四步：标签分配

训练程序选择部分候选预测，分别负责学习3只真实蜜蜂。

### 第五步：计算损失

假设预测结果存在以下问题：

```text
蜜蜂1：检测框向左偏
蜜蜂2：类别置信度过低
蜜蜂3：检测框尺寸过大
```

于是产生：

```text
box_loss
cls_loss
dfl_loss
```

### 第六步：反向传播

根据总损失计算：

$$
\frac{\partial L}{\partial \theta}
$$

得到每个参数的梯度。

### 第七步：更新参数

优化器根据梯度对参数进行微小修改。

### 第八步：学习效果累积

下一次看到相似图像时，模型可能会：

- 更容易识别蜜蜂纹理；
- 给蜜蜂更高的类别置信度；
- 预测出更加准确的检测框；
- 减少背景误检；
- 减少真实蜜蜂漏检。

经过大量Batch和Epoch后，模型逐渐形成稳定的蜜蜂检测能力。

---

## 二十三、模型文件中保存了什么

训练完成后的 `best.pt` 主要保存：

- 神经网络结构相关信息；
- 训练后的卷积核参数；
- BatchNorm参数；
- 检测头参数；
- 类别信息；
- 部分训练配置；
- 可能用于恢复训练的优化器状态等信息。

它并不是简单保存了所有训练图片。

模型真正保存的是：

> 从训练数据中学习得到的大量数值参数。

这些参数共同决定模型如何从输入图像中提取特征并产生检测结果。

---

## 二十四、常见误区

### 误区一：训练轮数越多，模型一定越好

错误。

训练轮数过少可能欠拟合，训练轮数过多可能过拟合。

应该根据验证集指标判断模型是否继续改善。

---

### 误区二：训练损失最低的模型一定最好

错误。

训练损失低只能说明模型更适合训练集。

真正部署时更关心验证集和测试集上的泛化性能。

---

### 误区三：best.pt一定是最后一轮模型

错误。

`best.pt` 是训练过程中验证性能最好的模型，可能来自中间某个Epoch。

---

### 误区四：单类别任务不需要分类损失

错误。

即使只有一个目标类别，模型仍需区分目标位置和背景位置。

---

### 误区五：YOLO训练时只学习检测框

错误。

模型同时学习：

- 视觉特征；
- 目标类别；
- 检测框位置；
- 检测框边界分布；
- 多尺度目标表示。

---

### 误区六：YOLO能够自动保持目标ID

不一定。

YOLO主要负责单帧目标检测。

跨帧ID保持通常需要ByteTrack、BoT-SORT等多目标跟踪算法。

---

### 误区七：推理时也需要真实标签

错误。

真实标签只用于训练和评价。

实际推理时，模型只需要输入图片或视频。

---

## 核心总结

YOLO目标检测训练可以概括为七个核心阶段：

```text
1. 准备带检测框标签的数据集
2. 加载模型结构和预训练参数
3. 读取Batch并执行数据增强
4. 前向传播产生候选预测
5. 将候选预测与真实标签进行匹配
6. 计算检测框、类别和DFL损失
7. 反向传播并由优化器更新参数
```

整个训练闭环为：

$$
\boxed{
\text{预测}
\rightarrow
\text{与标签比较}
\rightarrow
\text{计算损失}
\rightarrow
\text{反向传播}
\rightarrow
\text{更新参数}
}
$$

训练过程会在大量Batch和Epoch中不断重复。

最终得到的 `best.pt`，本质上是一组经过训练的神经网络参数。这些参数使模型能够将输入图像转换为：

```text
目标类别
+
目标检测框
+
预测置信度
```

对于智慧养蜂项目，YOLO检测通常是后续任务的基础：

$$
\boxed{
\text{YOLO目标检测}
\rightarrow
\text{多目标跟踪}
\rightarrow
\text{轨迹统计}
\rightarrow
\text{行为分析}
}
$$

---

## 相关笔记

- [[卷积神经网络]]
- [[YOLO]]
- [[目标检测]]
- [[数据增强]]
- [[前向传播]]
- [[反向传播]]
- [[损失函数]]
- [[优化器]]
- [[学习率]]
- [[IoU]]
- [[NMS]]
- [[Precision与Recall]]
- [[mAP]]
- [[过拟合]]
- [[迁移学习]]
- [[ByteTrack]]
- [[多目标跟踪]]
- [[蜜蜂行为分析]]

