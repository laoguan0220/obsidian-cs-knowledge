![[Pasted image 20260519143830.png]]
解释一下第一种
对于非负权的图 从任意节点出发最长路径一定延伸的直径的一端
理解这个就能明白 为什么要两次dfs 以及为什么适用于非负权
如果有负边权 那么可能就不会走所谓的直径组成的节点
需要dist数组和last数组 dist数组记录从头节点到该节点的距离 last$[i]$ 表示走到该节点的上一个节点是谁 也就是父亲结点
这个last数组的作用就是记录直径是怎么走过去的 记录路径节点信息
```cpp
void dfs(int u, int parent, long long dist) {
    if(dist > maxDist) {
        maxDist = dist;
        farNode = u;
    }
    for(auto &e : adj[u]) {
        if(e.to != parent) {
            dfs(e.to, u, dist + e.w);
        }
    }
}
```
第二种
树形dp方法
dp$[i]$ 的意思是以i为头结点向下延伸的最长长度是多少
dist$[u]$ ans$[u]$ ans$[u]$的意思是可以往两边去延伸的最大长度是多少
遍历整颗树即可 但是不能知道路径上的节点信息
