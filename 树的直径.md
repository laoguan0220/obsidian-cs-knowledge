![[Pasted image 20260519143830.png]]
解释一下第一种
对于非负权的图 从任意节点出发最长路径一定延伸的直径的一端
理解这个就能明白 为什么要两次dfs 以及为什么适用于非负权
如果有负边权 那么可能就不会走所谓的直径组成的节点
需要dist数组和last数组 dist数组记录从头节点到该节点的距离 last$[i]$ 表示走到该节点的上一个节点是谁 也就是父亲结点
这个last数组的作用就是记录直径是怎么走过去的 记录路径节点信息
```cpp
void dfs(int u, int parent, long long dist) {
    if(dist > maxDist) {
        maxDist = dist;
        farNode = u;
    }
    for(auto &e : adj[u]) {
        if(e.to != parent) {
            dfs(e.to, u, dist + e.w);
        }
    }
}
```
第二种
树形dp方法
dp$[i]$ 的意思是以i为头结点向下延伸的最长长度是多少
dist$[u]$ ans$[u]$ ans$[u]$的意思是可以往两边去延伸的最大长度是多少
遍历整颗树即可 但是不能知道路径上的节点信息
对于额外的节点 可以用反证法 假设有两条没有交点的直径去证明 
![[Pasted image 20260519200146.png]]
分析：
![[Pasted image 20260519202209.png]]
关于一些长度相等的直径可能的形式的模型 要记住
我们从end到last去遍历 去研究路径上的每个节点的 不走直径的路径的最大长度 如果是等于其到end的距离或者是其到start的距离 ，我们将它记录下来 也就是说对于每个节点 
1.我们首先进行两边dfs 建立好dist数组和last数组 告诉我它的起始点和终止点分别是谁
从last到start进行遍历 首先预处理每个节点的非路径上的子树的距离
![[Pasted image 20260519224220.png]]
