[[线段树]]
![[Pasted image 20260515132956.png]]
分析:
1.我们需要记录4个信息 sum和信息 最大值信息 次大值信息 最大值个数信息
2.首先要建树 注意一点 对于叶子节点 他没有什么次小值 我们可以认为 次小值是一个很小的数 取不到 可以将其视作"无“ 
3.update更新的时候 分三种情况 
(1) 比最大值还要大 剪枝
(2)比最大值小 比次大值大要将最大值进行更新 对sum进行更新
注意这里只需要更新命中的区间就可以了 不用下发 最大值信息本身也可以作为一个懒标记
那么在下发的时候 如果孩子的最大值比下发的值要大就进行更新 而且孩子的次大值一定不会受到影响  因为这种情况被情况三给处理掉了
(3) 如果比次大值还要小$\leq$  那么我们需要下发 更新所有的子节点
![[Pasted image 20260515152437.png]]
![[Pasted image 20260515163610.png]]![[Pasted image 20260515165637.png]]
一些比较关键的操作 update分了三种情况 如果这个数值比最大数都要大 那么直接剪枝
如果是第二种则需要给这个节点打上懒标记  更新它的这个最大值

注意第三种情况 有可能是任务没有全包 或者说呢值太小了 进入了第三种情况
无论哪种子情况 我们都要进行下发递归

其它的一些操作会不会导致势能的增加 如果会导致势能的增加 这个增加量是多少？
一些修改会导致最大值次大值发生变化  新标签的产生（也就是说sem为INT_MIN）

![[Pasted image 20260515163720.png]]
![[Pasted image 20260515164128.png]]
新增加的标签的大致分布 标签的数量为log(n)
回收一个标签要走一个树高 也就是$(\log n)^2$
setminlazy的懒更新 
addlazy的懒更新 并且设置优先级 
另一种写法 可以改成一个lazy
记录两个信息 最大值的增加值 其它值的增加值
如果要调用setmin操作 只需要让最大值的增加值变化 其他值增加值为0
如果是addlazy 最大值的增加值和其他值增加的一样多
当然如果写成一种写法 你得max只是查询信息了 不再作为懒标记 
并且增加了两个懒标记 maxAdd otherAdd 
记录下来某些节点的状态

对某个节点打懒标记的时候 
![[Pasted image 20260515171955.png]]
注意如果sem_i是最小值的话 那么你应该加上0 也就是说如果没有其他值的话 sem应该保持最小值不变 否则的话 有可能叶子结点就会造成逻辑上的有最小值 那么你就有可能update导致错误


