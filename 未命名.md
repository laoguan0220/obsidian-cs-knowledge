---
title: x86-64 栈帧、RBP、返回地址与 _start 入口分析
tags:
  - x86-64
  - 汇编
  - 栈帧
  - RBP
  - RSP
  - ELF
  - Linux
  - CSAPP
date: 2026-07-22
---

# x86-64 栈帧、RBP、返回地址与 `_start` 入口分析

## 一句话理解

> [!summary]
> `call` 负责把**返回地址**压入栈，`push %rbp; mov %rsp,%rbp` 负责建立经典[[栈帧]]，`ret` 最终从 `%rsp` 指向的位置取回返回地址；而 `_start` 不是通过 `call` 进入的，所以即使它建立了栈帧，也没有正常的返回地址。

---

# 1. 为什么需要理解栈帧

理解栈帧有助于分析：

- 函数调用与函数返回；
- 局部变量的存放位置；
- `%rsp` 和 `%rbp` 的区别；
- 返回地址如何保存和恢复；
- [[GDB调试]]中的调用栈；
- `_start`、`main` 和操作系统之间的关系；
- `call`、`leave`、`ret` 等汇编指令；
- 栈溢出、缓冲区溢出等底层问题。

栈帧的本质是：

> 每次函数调用期间，在栈中划分出一块区域，用于保存返回地址、上一层栈帧信息、局部变量以及其他临时数据。

需要注意：

> [!important]
> 函数不一定必须使用 `%rbp` 建立栈帧。现代编译器可能使用 `-fomit-frame-pointer` 省略帧指针，只依靠 `%rsp` 管理栈空间。

因此：

- 返回机制依赖返回地址和 `%rsp`；
- `%rbp` 只是经典栈帧中的辅助寄存器；
- `%rbp` 不是函数返回的必要条件。

---

# 2. 核心寄存器

## 2.1 `%rsp`：栈顶指针

`%rsp` 保存当前栈顶的地址。

x86-64 的栈向**低地址方向增长**：

```text
高地址
   ↑
   │
   │ 旧的数据
   │
   │ 新压入的数据
   ↓
低地址
```

执行一次 64 位 `push` 时：

$$
\mathrm{rsp}_{new}=\mathrm{rsp}_{old}-8
$$

然后将数据写入：

$$
[\mathrm{rsp}_{new},\mathrm{rsp}_{new}+7]
$$

例如，原来：

```text
rsp = 0x2000
```

执行：

```asm
push %rbp
```

之后：

```text
rsp = 0x1ff8
```

被压入的 64 位数据占用：

```text
0x1ff8 ～ 0x1fff
```

此时 `%rsp` 指向：

```text
0x1ff8
```

也就是这 8 字节数据的**最低地址**。

---

## 2.2 `%rbp`：经典栈帧基址

在传统栈帧中，函数通常执行：

```asm
push %rbp
mov  %rsp, %rbp
```

AT&T 语法中：

```asm
mov 源操作数, 目的操作数
```

因此：

```asm
mov %rsp, %rbp
```

表示：

```text
rbp = rsp
```

执行后，`%rbp` 固定指向当前函数栈帧中的某个基准位置。

经典布局通常满足：

```text
[rbp]     = 保存的调用者 rbp
[rbp + 8] = 返回地址
```

---

## 2.3 `%rip`：指令指针

`%rip` 保存 CPU 下一条要执行的指令地址。

执行普通函数返回时：

```asm
ret
```

会从 `%rsp` 指向的位置读取返回地址，并写入 `%rip`。

可以近似理解为：

```text
rip = *(uint64_t *)rsp
rsp = rsp + 8
```

> [!important]
> `ret` 直接使用的是 `%rsp`，而不是 `%rbp`。

---

# 3. 一个 64 位数在内存中如何存储

一个 64 位数需要 8 字节。

假设一个 64 位数从地址 `0x1000` 开始存储，则它占用：

```text
0x1000
0x1001
0x1002
0x1003
0x1004
0x1005
0x1006
0x1007
```

指向这个 64 位数的指针保存的是：

```text
0x1000
```

而不是：

```text
0x1007
```

因此，当：

```text
rsp = 0x1000
```

表示 `%rsp` 指向这个 64 位数据的**起始地址，也就是最低地址**。

---

## 3.1 小端序

x86-64 使用小端序。

例如：

```text
0x1122334455667788
```

从地址 `0x1000` 开始存储时：

```text
地址       字节
0x1000     88
0x1001     77
0x1002     66
0x1003     55
0x1004     44
0x1005     33
0x1006     22
0x1007     11
```

> [!note]
> “指针指向最低地址”与“小端序”是两个不同概念。
>
> - 指针保存数据区域的起始地址；
> - 小端序决定数据内部各字节的排列方式。

---

# 4. 普通函数的调用过程

假设：

```c
void foo(void)
{
}
```

调用者执行：

```asm
call foo
```

---

## 4.1 `call` 保存返回地址

`call foo` 可以近似理解为：

```text
rsp = rsp - 8
[rsp] = call 后面下一条指令的地址
rip = foo 的地址
```

因此，刚进入 `foo` 时：

```text
rsp → 返回地址
```

假设返回地址存放在：

```text
0x2000 ～ 0x2007
```

那么：

```text
rsp = 0x2000
```

---

## 4.2 `foo` 保存调用者的 `%rbp`

函数序言：

```asm
push %rbp
mov  %rsp, %rbp
```

假设刚进入 `foo` 时：

```text
rsp = 0x2000
rbp = 0x3000
```

执行：

```asm
push %rbp
```

之后：

```text
rsp = 0x1ff8
```

并且：

```text
0x1ff8 ～ 0x1fff
```

保存值：

```text
0x3000
```

然后执行：

```asm
mov %rsp, %rbp
```

得到：

```text
rbp = rsp = 0x1ff8
```

---

## 4.3 正确的经典栈帧图

假设图中上方为高地址、下方为低地址：

```text
                         高地址

rbp+15    ┌──────────────────────┐
          │                      │
          │       返回地址        │
rbp+8     └──────────────────────┘
            ↑
            返回地址的最低地址

rbp+7     ┌──────────────────────┐
          │                      │
          │   保存的调用者 rbp    │
rbp       └──────────────────────┘
            ↑
            新 rbp 指向的位置

rbp-1     ┌──────────────────────┐
          │      局部变量         │
          │        ...           │
rbp-N     └──────────────────────┘

                         低地址
```

对应关系：

```text
[rbp]     = 调用者的 rbp
[rbp + 8] = 返回地址
```

地址范围为：

$$
\text{保存的旧 rbp}:[rbp,rbp+7]
$$

$$
\text{返回地址}:[rbp+8,rbp+15]
$$

> [!warning]
> 若图中“高地址在上、低地址在下”，则 `%rbp` 箭头应指向保存旧 `%rbp` 区域的下边界，也就是该 8 字节区域的最低地址。
>
> `%rbp+8` 应指向返回地址区域的最低地址。

---

# 5. 普通函数如何返回

经典函数尾声通常是：

```asm
leave
ret
```

其中：

```asm
leave
```

等价于：

```asm
mov %rbp, %rsp
pop %rbp
```

---

## 5.1 `mov %rbp,%rsp`

执行前，`%rsp` 可能指向局部变量区域的更低地址。

执行：

```asm
mov %rbp, %rsp
```

后：

```text
rsp = rbp
```

栈顶恢复到保存旧 `%rbp` 的位置。

局部变量占用的栈空间被整体丢弃。

---

## 5.2 `pop %rbp`

`pop %rbp` 可以近似理解为：

```text
rbp = [rsp]
rsp = rsp + 8
```

执行后：

- `%rbp` 恢复成调用者的 `%rbp`；
- `%rsp` 向高地址移动 8 字节；
- `%rsp` 此时正好指向返回地址。

---

## 5.3 `ret`

`ret` 可以近似理解为：

```text
rip = [rsp]
rsp = rsp + 8
```

CPU 从栈顶读取返回地址，并跳回调用者。

完整过程：

```text
mov %rbp,%rsp
        ↓
rsp 回到保存旧 rbp 的位置
        ↓
pop %rbp
        ↓
恢复调用者 rbp
        ↓
rsp 指向返回地址
        ↓
ret
        ↓
返回地址写入 rip
```

> [!important]
> `%rbp` 可以帮助恢复栈帧，并间接使 `%rsp` 回到返回地址附近，但真正读取返回地址的是 `ret`，而 `ret` 读取的是 `[rsp]`。

---

# 6. `_start` 与普通函数的根本区别

普通函数通过：

```asm
call foo
```

进入。

而程序入口 `_start` 通常不是通过：

```asm
call _start
```

进入。

操作系统加载 [[ELF]] 可执行文件后，会根据 ELF 入口地址启动程序，可以简化理解为：

```text
rip = ELF 入口地址
rsp = 初始用户栈地址
```

若 ELF 入口为 `_start`，则：

```text
rip = _start
```

因此：

> `_start` 没有普通意义上的调用者，也没有由 `call` 压入的返回地址。

---

# 7. `_start` 初始时 `%rbp` 有没有值

CPU 中的 `%rbp` 寄存器始终包含 64 位二进制内容。

因此，进入 `_start` 时：

```text
rbp = 某个 64 位值
```

但是这个值通常没有定义好的程序语义。

可以表示为：

```text
rbp = B
```

其中 $B$ 是入口时 `%rbp` 中已有的位模式。

> [!note]
> “寄存器有值”不等于“寄存器中的值具有有效含义”。

对 `_start` 而言：

- `%rsp` 具有明确含义：指向初始用户栈；
- `%rip` 具有明确含义：指向入口指令；
- `%rbp` 中虽然有位模式，但不应假设它指向有效的上一层栈帧。

---

# 8. `_start` 的初始栈

在 Linux x86-64 环境中，初始用户栈大致包括：

```text
argc
argv[0]
argv[1]
...
NULL
envp[0]
envp[1]
...
NULL
辅助向量 auxv
...
```

简化表示：

```text
                    高地址

S+16     ┌────────────────────┐
         │ argv[1] 指针        │
S+8      ├────────────────────┤
         │ argv[0] 指针        │
S        ├────────────────────┤
         │ argc                │
         └────────────────────┘
           ↑
           rsp

                    低地址
```

刚进入 `_start` 时：

```text
rsp = S
[rsp] = argc
```

此时栈顶不是返回地址。

---

# 9. 具体例子：分析给定 `_start`

反汇编代码：

```asm
0000000000401000 <_start>:
  401000: f3 0f 1e fa    endbr64
  401004: 55             push   %rbp
  401005: 48 89 e5       mov    %rsp,%rbp
  401008: eb fe          jmp    401008 <_start+0x8>
```

设刚进入 `_start` 时：

```text
rsp = S
rbp = B
rip = 0x401000
```

其中：

- $S$ 是初始栈顶地址；
- $B$ 是入口时 `%rbp` 中原本的 64 位值。

---

## 9.1 `endbr64`

```asm
401000: endbr64
```

这条指令与当前讨论的栈帧基本无关。

执行后：

```text
rsp = S
rbp = B
```

栈内容不变。

---

## 9.2 `push %rbp`

```asm
401004: push %rbp
```

其效果为：

$$
rsp\leftarrow rsp-8
$$

$$
[rsp]\leftarrow rbp
$$

代入初始值：

$$
rsp=S-8
$$

并且：

$$
[S-8,S-1]\leftarrow B
$$

执行后：

```text
rsp = S - 8
rbp = B
```

栈结构：

```text
                      高地址

S+8       ┌────────────────────┐
          │ argv[0] 指针        │
S         ├────────────────────┤
          │ argc                │
S-1       ├────────────────────┤
          │                    │
          │ 初始 rbp 值 B       │
S-8       └────────────────────┘
            ↑
            rsp

                      低地址
```

此时 `%rsp` 指向保存值 $B$ 的 8 字节区域的最低地址。

---

## 9.3 `mov %rsp,%rbp`

```asm
401005: mov %rsp,%rbp
```

AT&T 语法为：

```text
mov 源, 目的
```

因此：

$$
rbp\leftarrow rsp
$$

执行后：

$$
rbp=rsp=S-8
$$

栈结构：

```text
                         高地址

rbp+16     ┌────────────────────┐
           │ argv[0] 指针        │
rbp+8      ├────────────────────┤
           │ argc                │
rbp+7      ├────────────────────┤
           │                    │
           │ 初始 rbp 值 B       │
rbp        └────────────────────┘
             ↑
             rbp
             rsp

                         低地址
```

因此：

```text
[rbp]     = B
[rbp + 8] = argc
```

这与普通函数不同。

普通函数经典栈帧：

```text
[rbp]     = 调用者的 rbp
[rbp + 8] = 返回地址
```

当前 `_start`：

```text
[rbp]     = 入口时原本的 rbp 值
[rbp + 8] = argc
```

原因是进入 `_start` 之前没有执行：

```asm
call _start
```

---

## 9.4 `jmp 401008`

```asm
401008: jmp 401008
```

这条指令不断跳回自身：

```text
0x401008
   ↓
0x401008
   ↓
0x401008
   ↓
...
```

因此程序进入无限循环。

它不会：

- 调用 `main`；
- 执行 `leave`；
- 执行 `ret`；
- 执行退出系统调用。

---

# 10. 代入一个具体地址

假设执行完：

```asm
push %rbp
mov  %rsp,%rbp
```

后，GDB 显示：

```text
rbp = 0x7fffffffdea8
rsp = 0x7fffffffdea8
```

由于 `push %rbp` 使 `%rsp` 减少了 8，因此进入 `_start` 前：

$$
S=0x7fffffffdea8+8
$$

所以：

```text
S = 0x7fffffffdeb0
```

栈结构：

```text
                               高地址

0x7fffffffdeb8   ┌────────────────────┐
                 │ argv[0] 指针        │
0x7fffffffdeb0   ├────────────────────┤
                 │ argc                │
0x7fffffffdeaf   ├────────────────────┤
                 │                    │
                 │ 初始 rbp 值 B       │
0x7fffffffdea8   └────────────────────┘
                   ↑
                   rbp
                   rsp

                               低地址
```

可以用 GDB 查看：

```gdb
info registers rbp rsp
x/2gx $rbp
```

其中：

```text
x/2gx $rbp
```

表示：

- 从 `%rbp` 指向的位置开始；
- 以 8 字节为一个单位；
- 连续查看两个 8 字节数据；
- 以十六进制显示。

理论上：

```text
第一个 8 字节：[rbp]     = B
第二个 8 字节：[rbp + 8] = argc
```

也可以分别查看：

```gdb
x/gx $rbp
x/gx $rbp+8
```

查看 `argc` 的十进制值：

```gdb
p/d *(unsigned long *)($rbp + 8)
```

---

# 11. 强行让这个 `_start` 返回会怎样

假设将无限循环改成：

```asm
leave
ret
```

完整代码类似：

```asm
_start:
    push %rbp
    mov  %rsp,%rbp
    leave
    ret
```

---

## 11.1 执行 `leave`

`leave` 等价于：

```asm
mov %rbp,%rsp
pop %rbp
```

执行前：

```text
rbp = rsp = S - 8
[rbp] = B
[rbp + 8] = argc
```

首先：

```asm
mov %rbp,%rsp
```

结果仍然是：

```text
rsp = S - 8
```

然后：

```asm
pop %rbp
```

得到：

```text
rbp = B
rsp = S
```

此时 `%rsp` 又指向初始栈中的 `argc`。

---

## 11.2 执行 `ret`

`ret` 从 `[rsp]` 取 8 字节作为返回地址：

```text
rip = [rsp]
```

但当前：

```text
[rsp] = argc
```

假设程序运行时：

```text
argc = 1
```

那么：

```text
rip = 0x1
```

CPU 将尝试执行地址 `0x1` 处的指令，通常导致：

```text
Segmentation fault
```

> [!danger]
> 给 `_start` 建立栈帧，并不会自动创造一个返回地址。
>
> 返回地址只有在进入目标前执行了 `call`，或者程序手动伪造了等价栈布局时才会存在。

---

# 12. `_start` 调用 `main`

虽然 `_start` 自己没有返回地址，但它可以调用其他函数。

简化模型：

```asm
_start:
    call main

    mov %rax, %rdi
    mov $60, %rax
    syscall
```

执行：

```asm
call main
```

时，CPU 会把 `call main` 后面下一条指令的地址压栈。

因此 `main` 有真实的返回地址。

调用关系：

```text
内核直接进入 _start
          ↓
_start 执行 call main
          ↓
main 执行
          ↓
main 执行 ret
          ↓
返回 _start 中 call main 后面的指令
          ↓
_start 执行 exit 系统调用
          ↓
进程结束
```

---

## 12.1 `main` 建立自己的栈帧

假设 `main` 开头是：

```asm
main:
    push %rbp
    mov  %rsp,%rbp
```

在执行 `call main` 前，假设：

```text
rbp = rbp_start
rsp = T
```

执行 `call main`：

```text
rsp = T - 8
[rsp] = main 的返回地址
```

进入 `main` 后执行：

```asm
push %rbp
```

保存的是当前寄存器中的：

```text
rbp_start
```

随后：

```asm
mov %rsp,%rbp
```

建立 `main` 自己的 `%rbp`。

此时：

```text
[main_rbp]     = 调用 main 时的 rbp
[main_rbp + 8] = main 的返回地址
```

因此 `main` 可以正常执行：

```asm
leave
ret
```

---

## 12.2 `main` 再调用其他函数

若 `main` 执行：

```asm
call foo
```

则：

- `call foo` 保存返回到 `main` 的地址；
- `foo` 的 `push %rbp` 保存 `main` 当前的 `%rbp`；
- `foo` 的 `mov %rsp,%rbp` 建立 `foo` 自己的栈帧。

于是形成经典的 `%rbp` 链：

```text
foo 的 rbp
    ↓
[foo 的 rbp] = main 的 rbp
    ↓
[main 的 rbp] = 上一层 rbp
```

这与[[函数调用约定]]、调试器回溯调用栈密切相关。

> [!note]
> 实际使用 glibc 的 Linux 程序中，`_start` 通常不是简单地直接调用 `main`，而是调用类似 `__libc_start_main` 的运行库启动函数，再由运行库完成初始化并调用 `main`。
>
> 但分析栈帧时，可以先使用 `_start → call main → main ret` 的简化模型理解。

---

# 13. 数学推导总结

设进入普通函数 `foo` 前，调用者执行：

```asm
call foo
```

执行 `call` 前：

$$
rsp=S
$$

`call` 压入返回地址：

$$
rsp=S-8
$$

$$
[rsp,rsp+7]=\text{return address}
$$

进入 `foo` 后执行：

```asm
push %rbp
```

得到：

$$
rsp=S-16
$$

$$
[rsp,rsp+7]=rbp_{caller}
$$

然后执行：

```asm
mov %rsp,%rbp
```

得到：

$$
rbp_{foo}=S-16
$$

所以：

$$
[rbp_{foo}]=rbp_{caller}
$$

$$
[rbp_{foo}+8]=\text{return address}
$$

---

对于 `_start`，设初始：

$$
rsp=S
$$

$$
rbp=B
$$

初始栈顶：

$$
[S,S+7]=argc
$$

执行：

```asm
push %rbp
```

得到：

$$
rsp=S-8
$$

$$
[S-8,S-1]=B
$$

执行：

```asm
mov %rsp,%rbp
```

得到：

$$
rbp=S-8
$$

因此：

$$
[rbp]=B
$$

$$
[rbp+8]=argc
$$

这说明：

$$
\boxed{[rbp+8]\text{ 的含义取决于函数是如何进入的}}
$$

对于普通 `call` 进入的函数：

$$
[rbp+8]=\text{return address}
$$

对于没有通过 `call` 进入的 `_start`：

$$
[rbp+8]=argc
$$

---

# 14. 常见误区

## 误区一：`%rbp` 指向保存数据的最高地址

错误理解：

```text
rbp 指向一个 8 字节数的最后一个字节
```

正确理解：

```text
rbp 指向这个 8 字节数据区域的最低地址
```

若：

```text
rbp = 0x1ff8
```

则 `[rbp]` 这个 64 位数据占用：

```text
0x1ff8 ～ 0x1fff
```

---

## 误区二：新 `%rbp` 指向返回地址

经典栈帧中，新 `%rbp` 指向：

```text
保存的旧 rbp 的最低地址
```

返回地址位于：

```text
rbp + 8
```

即：

```text
[rbp]     = 保存的旧 rbp
[rbp + 8] = 返回地址
```

---

## 误区三：函数返回依赖 `%rbp`

严格来说，函数返回依赖：

- `%rsp`；
- 栈上的返回地址；
- `ret` 指令。

`%rbp` 只是在经典栈帧中帮助恢复 `%rsp`。

即使完全不用 `%rbp`，也可以：

```asm
sub $32, %rsp
# 函数主体
add $32, %rsp
ret
```

---

## 误区四：建立栈帧就会产生返回地址

错误。

下面两条指令：

```asm
push %rbp
mov  %rsp,%rbp
```

只完成：

- 保存旧 `%rbp` 的值；
- 设置新的栈帧基址。

返回地址来自：

```asm
call
```

而不是来自：

```asm
push %rbp
```

---

## 误区五：`_start` 是函数，所以一定能 `ret`

`_start` 可以被汇编器、链接器和调试器当成函数符号看待，也可以拥有函数形式的代码和栈帧。

但其入口机制不是普通函数调用：

```text
内核直接设置 rip = _start
```

而不是：

```asm
call _start
```

因此它默认没有返回地址，不能按普通函数方式 `ret`。

---

## 误区六：`_start` 初始时 `%rbp` 没有值

CPU 寄存器始终包含二进制内容。

因此 `%rbp` 一定有一个 64 位位模式。

但它可能没有规定好的程序语义，不能将其理解为有效的调用者栈帧地址。

---

## 误区七：`[rbp+8]` 永远是返回地址

只有在以下条件成立时，经典布局中的 `[rbp+8]` 才是返回地址：

1. 函数通过 `call` 进入；
2. 函数执行了 `push %rbp`；
3. 随后执行了 `mov %rsp,%rbp`；
4. 中间没有特殊修改栈结构。

对于给定 `_start`：

```text
[rbp+8] = argc
```

而不是返回地址。

---

## 误区八：`call` 会自动保存 `%rbp`

`call` 只负责：

```text
压入返回地址
跳转到目标地址
```

保存调用者 `%rbp` 的动作通常由被调用函数自己完成：

```asm
push %rbp
```

因此：

```text
call            → 保存返回地址
push %rbp       → 保存调用者的帧指针
mov %rsp,%rbp   → 建立当前帧指针
```

---

# 15. 核心总结

> [!summary]
> 1. x86-64 栈向低地址增长。
> 2. 64 位 `push` 会让 `%rsp` 减少 8。
> 3. `%rsp` 和 `%rbp` 都指向数据区域的最低地址。
> 4. `call` 压入返回地址。
> 5. `push %rbp` 保存调用者的 `%rbp`。
> 6. `mov %rsp,%rbp` 建立当前函数的经典栈帧。
> 7. 普通函数经典布局中，`[rbp]` 是旧 `%rbp`，`[rbp+8]` 是返回地址。
> 8. `leave` 恢复 `%rsp` 和旧 `%rbp`。
> 9. `ret` 从 `[rsp]` 取得返回地址，不直接读取 `%rbp`。
> 10. `_start` 由操作系统直接进入，不是通过 `call` 进入。
> 11. `_start` 即使执行 `push %rbp; mov %rsp,%rbp`，也只是建立了形式上的栈帧，并不会产生返回地址。
> 12. 在给定 `_start` 例子中，执行函数序言后，`[rbp]` 是入口时原本的 `%rbp` 值，`[rbp+8]` 是 `argc`。
> 13. `_start` 通常通过退出系统调用结束进程，而不是通过 `ret` 返回。
> 14. `_start` 调用 `main` 时，`call main` 会给 `main` 保存真实的返回地址，所以 `main` 能正常返回到启动代码。

最终可以用一条主线记忆：

```text
call 保存返回地址
        ↓
push %rbp 保存上一层帧指针
        ↓
mov %rsp,%rbp 建立当前栈帧
        ↓
leave 恢复栈帧
        ↓
ret 从 %rsp 取回返回地址
```

而 `_start` 的特殊性是：

```text
没有 call _start
        ↓
没有 _start 的返回地址
        ↓
即使有 rbp 栈帧，也不能正常 ret
        ↓
通过 exit 系统调用结束进程
```

相关笔记：

- [[x86-64汇编]]
- [[栈帧]]
- [[函数调用约定]]
- [[ELF文件格式]]
- [[进程启动]]
- [[GDB调试]]
- [[虚拟内存]]
- [[系统调用]]