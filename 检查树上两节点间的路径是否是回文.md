![[Pasted image 20260518223854.png]]
分析：
1.这道题可以用树上倍增和字符串哈希去做 
先预处理 所有从头结点出发到达该节点的字符串的哈希值
树形的字符串转化成线性的字符串
对于任意一条查询 查询这两个节点的最近公共祖先 首先确定两个节点与公共祖先组成的字符串
字符串a和字符串b，有两种情况，一种是以a为高位，b为低位，另一种是a为低位，b为高位 看两个字符串哈希值是否相等证明是否为回文串
2.设了两个数组：
$stup[u][p]和stdown[u][p]$ 含义是从u节点开始往上走$2^p$步得到的字符串的哈希值 和从u节点出发往下走$2^p$步得到的字符串的哈希值
关键是状态如何进行转移
注意走的步数要不要包含初始节点 状态要设计好 否则会走重了就麻烦了
