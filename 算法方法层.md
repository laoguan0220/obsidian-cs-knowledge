## 1 目标状态假设 + 障碍分析 + 最小修复(瓶颈分析)
对于[[CF708C]]这道题来讲
假设点 u 可以成为重心->检查它为什么不是重心发现唯一的问题：某一侧连通块太大->看一次删边加边能不能把这块修小 ->得到需求，求两大联通块里能切除的子树的最大大小且$\leq \frac{n}{2}$ 的->
这个结果可以认为是遍历删除所有的边得到的->对每个节点都遍历两大联通块的所有边求解复杂度太高->分析本节点可不可以由其它节点递推得到->动态规划减少时间复杂度 
## 2 在有重复结构的问题中，建立“状态之间的依赖图”，然后按依赖顺序传播最优值、计数值或可行性。(动态规划的理解)

我不能直接贪心决定局部选择，
因为当前选择会影响未来；
但整个问题又有重复子结构，
所以可以把不同“局面”抽象成状态。
==状态是对历史的压缩。
转移是对选择的抽象。
计算顺序是对依赖关系的尊重。==
用“局面等价”消除重复，用“局部信息”承载全局最优。
## 3.