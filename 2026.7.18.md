```dataset.yaml # 
seed: 42                          # 全局随机种子，保证划分可复现
frame_step: 10                    # 抽帧步长（每 10 帧取一帧），约 3 FPS（30 FPS 视频）
image_extension: .jpg             # 抽帧输出的图片格式
raw_root: data/raw                # 原始视频存放路径（只读）
processed_root: data/processed    # 抽帧后图片存放路径
annotation_root: data/annotations # 标注文件存放路径
scenes:
  outside:                        # 巢外场景
    videos: [A-5-1, A-5-2, A-5-3, A-5-4]
    difficulties: [4, 3, 3, 2]    # 难度依次递减
  inside:                         # 巢内场景
    videos: [B-5-1, B-5-2, B-5-3, B-5-4]
    difficulties: [2, 4, 3, 2]
split:
  strategy: temporal_blocks       # 按连续时间块划分，防止帧泄漏
  train: 0.75                     # 训练集 75%
  val: 0.25                       # 验证集 25%
```
#### 抽帧步长 `frame_step: 10`每 10 帧取一帧，其他 9 帧不要了吗？

**是的，丢弃了**。但这在目标检测任务中很常见，原因如下：

- 相邻帧之间画面变化极小（30 FPS 下两帧间隔 33ms），标注全部帧效率极低且信息冗余。
- 抽帧后的数据仍然能覆盖完整的运动过程，只是时间分辨率降低了。
- 跟踪算法（如 ByteTrack）在推理时是对**原始全帧率视频**运行的，训练时只用了抽帧图片来做检测模型的训练。

> **注意：** 训练时用抽帧图片训练检测模型，推理时用全帧率视频跑检测+跟踪，两者是独立的环节。

####  FPS 是什么？

**FPS = Frames Per Second（每秒帧数）**，即视频每秒钟包含的画面数量。

- 30 FPS → 每秒 30 张画面
- 帧间隔 ≈ 33 ms
```inference.yaml
model: models/outside/bee.onnx    # ONNX 模型路径（示例路径）
input_size: 640                   # 模型输入尺寸（像素）
confidence: 0.25                  # 检测置信度阈值
iou: 0.45                         # NMS IoU 阈值
require_cuda: true                # 强制要求 GPU 推理，不得静默降级 CPU
class_id: 0                       # 类别 ID 固定为 0
label: bee                        # 类别标签固定为 "bee"
```

## yolo标注数据归一化的好处
### 1. 消除图像尺寸差异，实现“尺度不变性”

这是最根本的原因。现实世界中的图片尺寸千奇百怪（比如 1920x1080、4000x3000、甚至 500x500）。

- **如果不归一化**：模型需要学习不同尺寸下的绝对坐标。一张 4000 像素宽的图片，物体的 x 坐标可能是 2000；而在一张 1000 像素宽的图片里，同一个物体的 x 坐标可能只有 500。模型需要花费大量算力去理解“这两个坐标其实代表同一个相对位置”。
- **归一化后**：所有的坐标都被映射到了 `[0, 1]` 之间。无论原图多大，物体在画面正中心的坐标永远是 `(0.5, 0.5)`。这让模型只关注物体的**相对位置**，而不是绝对像素值，极大地降低了学习难度。

### 2. 加速模型收敛（优化神经网络训练）

神经网络在进行梯度下降优化时，对输入数据的数值范围非常敏感。

- 如果输入数据的数值范围很大（比如几千、几万的像素值），会导致损失函数的等高线变得非常狭长，模型在寻找最优解时容易“震荡”，收敛速度极慢。
- 将坐标和宽高都归一化到 `[0, 1]` 之间，使得各个维度的数据分布更加均匀，损失函数的地形更平滑，模型能更快、更稳定地找到最优解。

### 3. 方便构建 YOLO 的网格（Grid）机制

YOLO 算法的核心思想是将图像划分成 S×SS×S 的网格（比如 YOLOv3 中的 13x13，YOLOv5 中的 80x80）。

- 在 YOLO 的预测逻辑中，模型预测的边界框（Bounding Box）是相对于它所在的网格单元（Grid Cell）的偏移量，以及相对于先验框（Anchor）的缩放比例。
- 因为 YOLO 的输出特征图尺寸是固定的，使用 `[0, 1]` 的归一化坐标可以非常方便地与特征图的网格尺寸进行乘除运算，从而在代码实现上极其简洁。
	[[yolo网格机制]]
### 4. 增强模型的泛化能力

使用归一化坐标训练的模型，具有极强的**跨分辨率泛化能力**。

- 假设你用 1920x1080 的图片训练好了一个模型。当你在推理阶段遇到一张 4K 分辨率的图片时，因为模型学到的是“相对比例”，它可以直接把 4K 图片的坐标归一化后送入网络，或者将网络输出的归一化结果直接乘以 4K 的宽高，而无需重新训练模型。
## 关于[[ONNX]]模型 
[[yolo训练阶段和推理阶段之间的区别]]
[[yolo训练过程]]
```behavior.yaml
fps_fallback: 30.0                # 视频帧率后备值（无法从元数据读取时使用）

quality:                          # --- 质量控制门限 ---
  minimum_track_seconds: 0.5       # 有效轨迹最低时长（秒），短于此的轨迹丢弃
  minimum_detection_confidence: 0.25  # 检测置信度最低门限
  minimum_pose_confidence: 0.5     # 姿态（头/腹关键点）置信度最低门限
  minimum_valid_pose_ratio: 0.7    # 轨迹中有效姿态帧占比低于此值则排除
  maximum_frame_gap: 1             # 轨迹中最大允许断帧数（1 帧缺失可插值）

individual:                       # --- 个体级行为指标 ---
  smoothing_window_frames: 5       # 轨迹平滑窗口（帧数），用于滑动平均去噪
  active_speed_body_lengths_per_second: 0.2  # 活跃判定门限（体长/秒），低于此视为静止
  interaction_distance_body_lengths: 1.5     # 交互距离门限（体长倍数）
  interaction_minimum_seconds: 0.2           # 交互最小时长，短于此不统计

group:                            # --- 群体级行为指标 ---
  aggregation_window_seconds: 60   # 群体指标聚合时间窗口（秒）
  heatmap_rows: 12                 # 密度热力图行数
  heatmap_columns: 16              # 密度热力图列数
  crossing_cooldown_seconds: 1.0   # 巢口进出检测的冷却时间，防止重复计数

cross_scene:                      # --- 跨场景关联 ---
  require_synchronized_timestamps: true  # 要求时间戳同步
  maximum_lag_windows: 5                 # 最大允许时间偏移窗口数
```
