[[线段树维护区间最值和历史最值问题]]
1.$[l,r]$范围内的所有数v和x取最小的 
2.查询$[l,r]$范围内的累加和
3.查询$[l,r]$范围内的最大值
维护的信息 
maxnum sum cnt最大值出现的次数 sem次大值的数值
build建树 对要维护的信息进行初始化 尤其是对叶子节点
update然后对应着三种情况（对于包含在内的节点怎么修改？）
lazy需不需要懒标记并且进行修改？down 如果下发懒标记 怎么下发？ 懒标记是谁？
up怎么由子节点得到父节点的信息
query怎么查询需要的信息
1.$[l,r]$范围内的所有数v和x取最小的 
2.查询$[l,r]$范围内的累加和
3.查询$[l,r]$范围内的最大值
4.$[l,r]$范围加上一个数v
维护的信息 
maxnum sum cnt最大值出现的次数 sem次大值的数值
///maxadd otheradd
build建树 对要维护的信息进行初始化 尤其是对叶子节点
update然后对应着三种情况（对于包含在内的节点怎么修改？）
lazy需不需要懒标记并且进行修改？down 如果下发懒标记 怎么下发？  
up怎么由子节点得到父节点的信息
query怎么查询需要的信息
1.$[l,r]$范围内的所有数v和x取最小的 
2.查询$[l,r]$范围内的累加和
3.查询$[l,r]$范围内的最大值
4.$[l,r]$范围加上一个数v
5.1-4的每次操作都会生成一个新数组B B的数值是所有数组的数值的最大值
维护的信息 
maxnum sum cnt最大值出现的次数 sem次大值的数值
maxadd otheradd 
maxhistory maxaddtop maxothertop
build建树 对要维护的信息进行初始化 尤其是对叶子节点
update然后对应着三种情况（对于包含在内的节点怎么修改？）
lazy需不需要懒标记并且进行修改？down 如果下发懒标记 怎么下发？ 懒标记是谁？
up怎么由子节点得到父节点的信息
query怎么查询需要的信息

