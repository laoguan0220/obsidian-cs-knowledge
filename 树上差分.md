# 树上差分概念与分类

树上差分是一种在树上进行路径加法的技巧，将一次对路径上所有点（或边）进行加（或减）操作，转化为对几个节点进行修改，再通过一次后续 DFS 累加（前缀和）得到最终结果。其基本思想类似一维数组的差分：在区间两端做加减操作，然后做前缀和。对于树，上下关系不固定，需要借助最近公共祖先（LCA）等信息来“抵消”多余的影响。树上差分主要分为**点上差分**和**边上差分**：
[[树上点差分]]
- **点上差分**：若需要对路径上所有**节点**的权值加上 $d$，可以令对于查询 $(x,y)$：  
  $$a_x\ +=\ d,\quad a_y\ +=\ d,\quad a_{\text{lca}(x,y)}\ -\!=\ d,\quad a_{\text{parent}(\text{lca}(x,y))}\ -\!=\ d.$$  
  然后对树做一次从根到叶的 DFS，累加子节点到父节点的差分值，每个节点的最终值即为其点权增加后的结果【8†L106-L110】。上述公式的意义是，`a_x += d, a_y += d` 会让路径 $x\to y$ 上的所有点（包括 $x,y$）都被加了 $d$；然而对于 LCA 及其以上节点会多加一份，故要在 $\mathrm{lca}(x,y)$ 和其父节点处各减去 $d$ 来抵消额外影响【8†L106-L110】。

- **边上差分**：若需要对路径上所有**边**的权值加上 $d$，可以先将每个边的权值看作其**深度更深的端点**的“点权”。对于查询 $(x,y)$，可令：  
  $$a_x\ +=\ d,\quad a_y\ +=\ d,\quad a_{\text{lca}(x,y)}\ -\!=\ 2d.$$  [[树上边差分]]
  然后同样做一次 DFS 累加，节点的点权即代表该节点与其父边的边权增加的结果【9†L128-L136】。这里在 LCA 处减去 $2d$，是因为路径上的两条分支各加了一份 $d$，要全部抵消掉。

综上，树上差分可快速处理“对若干条根路径（或一般路径）上的节点/边权统一加减”的操作，将复杂的路径操作转为常数次修改和一次 DFS。其时间复杂度一般为 $O(n+m)$，适用于节点或查询数量较大、树链剖分难以胜任的场景【9†L156-L159】。例如，洛谷题单将本题组 P3128、P3258、P2680 都列为了树上差分练习题【13†L25-L29】，这三题都可以借助树上差分思想求解。

## P3128 [USACO15DEC] 最大流 (Max Flow)

**题意概要**（参考【1†L23-L31】）：给定一棵 $N$ 个节点的树（$N\le 5\times10^4$），有 $K$ 条询问（$K\le10^5$），每条询问给出两个节点 $s,t$，表示在树上从 $s$ 到 $t$ 的简单路径上运送牛奶，每次运送计 $1$ 单位牛奶。要求计算在所有路径叠加后，每个节点被经过的次数（即“流量”）最大值。注意路径包含端点。

**解题思路**：这是经典的“对树上路径加 1”问题，属于点上差分的应用。我们取任意节点（如 1）为根，先预处理树的深度和父结点，以及 LCA。对于每条查询 $(s,t)$，令
``` 
diff[s] += 1;
diff[t] += 1;
diff[lca(s,t)] -= 1;
diff[parent[lca(s,t)]] -= 1;
```
这样做的依据是点上差分公式【8†L106-L110】。然后，对树做一次从根向下的 DFS：对于节点 $u$，它的最终值 `val[u] = diff[u] + sum(val[v] for all children v of u)`。这时每个节点 `val[u]` 就是所有查询路径经过 $u$ 的次数。我们取最大值即为答案。该方案时间为 $O((N+K)\alpha)$ 或 $O((N+K)\log N)$（主要在 LCA 预处理和查询上）。

**关键公式**：对于每个询问 $(s,t)$，
> ```cpp
> diff[s]++; diff[t]++;
> diff[lca(s,t)]--; diff[parent[lca(s,t)]]--;
> ```
最后一次 DFS 累加得到各节点的 “流量”。【8†L106-L110】【4†L53-L60】

**代码实现**（C++，重点注释）： 

```cpp
#include <bits/stdc++.h>
using namespace std;
static const int MAXN = 50000 + 5;
int N, K;
vector<int> g[MAXN];
int parent[17][MAXN], depth[MAXN];
long long diff[MAXN], ans=0;

// DFS 计算深度和父节点（parent[0][u]）
void dfs1(int u, int p) {
    parent[0][u] = p;
    for(int v: g[u]) {
        if (v == p) continue;
        depth[v] = depth[u] + 1;
        dfs1(v, u);
    }
}

// LCA 预处理
void initLCA() {
    for(int k = 1; k < 17; k++)
        for(int i = 1; i <= N; i++)
            parent[k][i] = parent[k-1][ parent[k-1][i] ];
}

// 求 LCA(u,v)
int LCA(int u, int v) {
    if (depth[u] < depth[v]) swap(u, v);
    // 把 u 提到和 v 同深度
    int diff = depth[u] - depth[v];
    for(int k = 0; k < 17; k++)
        if (diff & (1<<k))
            u = parent[k][u];
    if (u == v) return u;
    for(int k = 16; k >= 0; k--) {
        if (parent[k][u] != parent[k][v]) {
            u = parent[k][u];
            v = parent[k][v];
        }
    }
    return parent[0][u];
}

// DFS 累加 diff 值：遍历后，把子节点累加到父节点
long long dfs2(int u, int p) {
    long long cnt = diff[u];
    for(int v: g[u]) {
        if (v == p) continue;
        cnt += dfs2(v, u);
    }
    ans = max(ans, cnt);  // 更新最大值
    return cnt;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> N >> K;
    // 读边并建树
    for(int i = 1; i <= N; i++) g[i].clear();
    for(int i = 1; i < N; i++) {
        int x, y; cin >> x >> y;
        g[x].push_back(y);
        g[y].push_back(x);
    }
    depth[1] = 0;
    dfs1(1, 0);       // 以 1 为根计算父节点和深度
    initLCA();        // 预处理 LCA

    // 处理每个查询：树上点差分
    for(int i = 0; i < K; i++){
        int s, t; cin >> s >> t;
        int l = LCA(s, t);
        diff[s] += 1;
        diff[t] += 1;
        diff[l] -= 1;
        if (parent[0][l] != 0) {
            diff[parent[0][l]] -= 1;
        }
    }

    // 第二次 DFS 累加 diff，并更新最大值
    dfs2(1, 0);
    cout << ans << "\n";
    return 0;
}
```

**代码说明**：我们建立树的邻接表 `g`，用 `dfs1` 计算父节点 `parent[0][u]` 和深度。接着用倍增预处理 LCA（函数 `initLCA`）。每次询问取 LCA，更新 `diff` 数组：`diff[s]++`, `diff[t]++`, `diff[l]--`, `diff[parent(l)]--`，其中 `parent(l)` 指 LCA 的父节点（根的父节点设置为 0，注意检查）。最后调用 `dfs2` 从根向下累加：每个节点返回自己的子树贡献和自身 `diff[u]`，并维护全局最大值 `ans`。结果即为经过最多查询的节点权值【8†L106-L110】【4†L53-L60】。

## P3258 [JLOI2014] 松鼠的新家

**题意概要**：给定一棵有 $n$ 个节点的树和一个访问序列 $a_1, a_2, \dots, a_n$（其实 $a$ 是个排列）。松鼠从 $a_1$ 依次走到 $a_2, a_3, \dots, a_n$，每经过一个节点（包括路径上的中间节点和到达的终点）时可以拿一颗糖果。要求计算保证松鼠每次到达都能拿到糖果的前提下，每个房间（节点）需要准备多少糖果。注意，最后一次到达 $a_n$ 时不取糖果（因为那里是宴会）。输出每个节点需要准备的糖果数。

**解题思路**：其实此题等价于：对于序列中连续的节点对 $(a_i, a_{i+1})$（$1 \le i < n$），都加上1
```cpp
#include<bits/stdc++.h>

using namespace std;

int n;

const int N=3e5+10;

int fa[N][20];

int depth[N];

vector<int>adj[N];

int val[N];

int arr[N];

int log2(int x)

{

    int res=-1;

    while(x)

    {

        x=x>>1;

        res++;

    }

    return res;

}

void dfs(int rt,int rf)

{

    depth[rt]=depth[rf]+1;

    fa[rt][0]=rf;

    for(int i=0;i<adj[rt].size();i++)

    {

        if(adj[rt][i]!=rf)

        {

            dfs(adj[rt][i],rt);

        }

    }

}

int lca(int x,int y)

{

    if(depth[x]<depth[y])

    {

        swap(x,y);

    }

    for(int i=log2(depth[x]-depth[y]);i>=0;i--)

    {

        if(depth[fa[x][i]]>=depth[y])

        {

            x=fa[x][i];

        }

    }

    if(x==y)

    return x;

    for(int i=log2(depth[x]);i>=0;i--)

    {

        if(fa[x][i]!=fa[y][i])

        {

            x=fa[x][i];

            y=fa[y][i];

        }

    }

    return fa[x][0];

}

void dfs1(int s,int t)

{

    val[s]++;

    val[t]++;

    int pa=lca(s,t);

    val[pa]--;

    val[fa[pa][0]]--;

}

void dfs2(int rt,int rf)

{

    for(int i=0;i<adj[rt].size();i++)

    {

        if(adj[rt][i]!=rf)

        {

            dfs2(adj[rt][i],rt);

            val[rt]+=val[adj[rt][i]];

        }

    }

}

int main()

{

    cin>>n;

    for(int i=1;i<=n;i++)

    {

        cin>>arr[i];

    }

    for(int i=1;i<n;i++)

    {

        int x,y;

        cin>>x>>y;

        adj[x].push_back(y);//这里x存的是a_i的编号

        adj[y].push_back(x);

    }

    dfs(arr[1],0);

    for(int j=1;j<20;j++)

    {

        for(int i=1;i<=n;i++)

        {

            fa[i][j]=fa[fa[i][j-1]][j-1];

        }

    }

    for(int i=1;i<n;i++)

    {

        dfs1(arr[i],arr[i+1]);

        // cout<<arr[i]<<" "<<fa[arr[i+1]][0]<<endl;

    }

    dfs2(arr[1],0);

    for(int i=2;i<=n;i++)

    {

        val[arr[i]]--;

    }

    for(int i=1;i<=n;i++)

    {

        cout<<val[i]<<endl;

    }

}
```


## P2680 [NOIP2015 提高组] 运输计划

**题意概要**（参考【11†L13-L18】）：给定一棵 $n$ 个节点的树（星球），有 $m$ 条运输计划（$m\le 3\times10^5$），每条计划是从 $u_i$ 到 $v_i$ 的最快路径。树的每条边 $j$ 有时间权值 $t_j$。公司可以选择将任意**一条边**改造为“虫洞”（权值变为 0）。所有 $m$ 条计划同时出发，求完成所有计划的**最短时间**，即所有路径中的最大耗时的最小值。

换言之，如果不改造边，最坏计划时间为所有 $\mathrm{dist}(u_i,v_i)$ 的最大值；改造一条边后，任何路径中经过该边的耗时减少该边的 $t_j$，其余路径不变。我们要选择一条边使得所有计划完成所需时间（即改造后所有单个路径耗时的最大值）最小。【11†L13-L18】【11†L27-L29】

**解题思路概括**：首先预处理树的 LCA 和任意两点距离，可以用一次 DFS 记录节点到根的距离，再用 `dist(u,v) = dis[u]+dis[v]-2*dis[lca(u,v)]` 求各计划距离。设 $D=\max_i d_i$ 为最坏计划时间，$D_2$ 为次大。若只有一条计划最坏，则可尝试将该计划路径上的某条重边改造；若有多条计划并列最坏，需要找到所有这些最长路径的公共边。

一种常见方法是二分答案：设中间时间 $T$，找出所有原始距离 $d_i > T$ 的“超时”计划。用树上差分统计所有这些计划经过的节点和边。例如，对每个超时计划 $(u,v)$ 做 `diff[u]++`, `diff[v]++`, `diff[lca(u,v)] -= 2`（这里是边上差分思想，但简化为节点上差分，统计每个边被经过的次数【6†L58-L62】）。累计之后，如果存在一条边（即它对应的较深端点）被所有超时计划经过，则把这条边的时间权 $t_j$ 减掉，可以使所有这些计划在 $T$ 内完成。具体可检查：令 $k$ 为超时计划数，求这些计划中最长时间超出量 $need=\max(d_i-T)$；如果某条边上有 `cnt = k`（所有超时计划都经过）且 $t_j \ge need$，则可行。通过二分不断缩小 $T$，复杂度 $O((n+m)\log C)$。详细解法见【6†L58-L62】。

*注意*：该题思路较复杂，关键在于使用树上差分统计经过的计划数并结合二分判断可否将某边改为虫洞。

**代码实现**（C++，核心注释）： 

```cpp
#include <bits/stdc++.h>
using namespace std;
static const int MAXN = 300000 + 5;
int n, m;
struct Edge { int to, w; };
vector<Edge> tree[MAXN];
int parent[19][MAXN], depth[MAXN];
long long distRoot[MAXN];
vector<pair<int,int>> queries;
long long diff3[MAXN];
int cntExceed;

// dfs1：计算 parent[0][u]、depth、以及 u 到根的距离 distRoot[u]
void dfs1(int u, int p) {
    parent[0][u] = p;
    for(auto &e: tree[u]) {
        int v = e.to, w = e.w;
        if (v == p) continue;
        depth[v] = depth[u] + 1;
        distRoot[v] = distRoot[u] + w;
        dfs1(v, u);
    }
}

// 预处理 LCA
void initLCA(int n) {
    for(int k = 1; k < 19; k++){
        for(int i = 1; i <= n; i++){
            parent[k][i] = parent[k-1][ parent[k-1][i] ];
        }
    }
}

// 求 LCA(u,v)
int LCA(int u, int v) {
    if (depth[u] < depth[v]) swap(u, v);
    int diff = depth[u] - depth[v];
    for(int k = 0; k < 19; k++){
        if (diff & (1<<k)) {
            u = parent[k][u];
        }
    }
    if (u == v) return u;
    for(int k = 18; k >= 0; k--){
        if (parent[k][u] != parent[k][v]){
            u = parent[k][u];
            v = parent[k][v];
        }
    }
    return parent[0][u];
}

// 计算两点间距离
long long calcDist(int u, int v) {
    int l = LCA(u, v);
    return distRoot[u] + distRoot[v] - 2*distRoot[l];
}

// dfs2：累计 diff3，用于统计经过子树向上累积
void dfs2(int u, int p){
    for(auto &e: tree[u]) {
        int v = e.to;
        if (v == p) continue;
        dfs2(v, u);
        diff3[u] += diff3[v];
    }
}

bool check(long long T){
    // 找出所有距离 > T 的计划
    vector<long long> excess;
    excess.reserve(m);
    for(int i = 0; i < m; i++){
        long long d = calcDist(queries[i].first, queries[i].second);
        if (d > T) {
            excess.push_back(d - T);
            int u = queries[i].first, v = queries[i].second;
            int l = LCA(u, v);
            diff3[u]++; diff3[v]++; diff3[l] -= 2;
        }
    }
    if (excess.empty()) {
        // 所有路径都 <= T，无需虫洞即可完成
        // 恢复 diff3 为 0（清理）
        for(int i = 0; i < m; i++){
            int u = queries[i].first, v = queries[i].second;
            int l = LCA(u, v);
            if(calcDist(u, v) > T){
                diff3[u]--; diff3[v]--; diff3[l] += 2;
            }
        }
        return true;
    }
    int k = excess.size();
    long long need = *max_element(excess.begin(), excess.end());
    // 累加 diff3，从任意根开始（选 1）
    dfs2(1, 0);
    // 寻找是否有节点 u 满足 diff3[u] == k 且 (u->parent[u]) 边的权 >= need
    bool ok = false;
    for(int i = 2; i <= n; i++){
        // i 表示节点，parent[0][i] 表示 i 的父节点
        if (diff3[i] == k) {
            // 找到 i 到父节点的边权
            // 在 dfs1 中 distRoot[u] + w = distRoot[v], 故 w = distRoot[i] - distRoot[parent]
            long long w = distRoot[i] - distRoot[parent[0][i]];
            if (w >= need) {
                ok = true;
                break;
            }
        }
    }
    // 恢复 diff3 数组（回滚之前加的）
    for(int i = 0; i < m; i++){
        long long d = calcDist(queries[i].first, queries[i].second);
        if (d > T) {
            int u = queries[i].first, v = queries[i].second;
            int l = LCA(u, v);
            diff3[u]--; diff3[v]--; diff3[l] += 2;
        }
    }
    return ok;
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> n >> m;
    for(int i = 1; i <= n; i++){
        tree[i].clear();
        distRoot[i] = 0;
        for(int k = 0; k < 19; k++) parent[k][i] = 0;
        diff3[i] = 0;
    }
    // 读边
    for(int i = 1; i < n; i++){
        int a, b, t; cin >> a >> b >> t;
        tree[a].push_back({b,t});
        tree[b].push_back({a,t});
    }
    // 计算父节点和距离
    depth[1] = 0;
    dfs1(1, 0);
    initLCA(n);

    // 读查询
    queries.resize(m);
    for(int i = 0; i < m; i++){
        cin >> queries[i].first >> queries[i].second;
    }
    // 先计算所有路径距离，记录最大值范围
    long long lo = 0, hi = 0;
    for(int i = 0; i < m; i++){
        long long d = calcDist(queries[i].first, queries[i].second);
        hi = max(hi, d);
    }
    // 二分答案
    long long ans = hi;
    while(lo <= hi){
        long long mid = (lo + hi) / 2;
        // 清除 diff3 数组（因为 check 中会修改）
        for(int i = 1; i <= n; i++) diff3[i] = 0;
        if (check(mid)) {
            ans = mid;
            hi = mid - 1;
        } else {
            lo = mid + 1;
        }
    }
    cout << ans << "\n";
    return 0;
}
```

**代码说明**：此代码首先通过 DFS 和 LCA 预处理得到任意两点距离 `calcDist(u,v)`。我们二分最坏完成时间 `T`。在 `check(T)` 中，对所有距离 $>T$ 的计划使用树上**节点差分**统计：`diff3[u]++`, `diff3[v]++`, `diff3[lca] -= 2`【6†L58-L62】。DFS 累加后，每个节点 `diff3[x]` 表示经过 `x` 节点到父节点边的超时计划数量。如果存在一个节点 $x$ 使得 `diff3[x]` 等于所有超时计划的数量 $k$，说明所有超时计划都经过这条边（$x$ 到 `parent[x]` 的边）；若该边的原权值 $t_j \ge \max(d_i - T)$，则将此边改造为虫洞后可以让所有超时计划时间 $\le T$，返回 `true`。否则返回 `false`。通过二分查找最小可行 $T$。该方法结合了树上差分与二分思路【6†L58-L62】【11†L13-L18】。

## 三题本质比较与思路总结

以上三题本质上都属于在树上处理“对若干路径进行加权”或“查询路径属性”的问题，都可借助**树上差分**这一技巧来实现高效统计。其中：

- **题1（Max Flow）和题2（Squirrel’s New Home）**：都是将路径上的节点访问次数累加的问题，属于**点上差分**的经典应用。解法几乎相同：对每个查询路径的两个端点和它们的 LCA 做差分操作【8†L106-L110】；再做一次 DFS 从下往上（或上往下）累加即可得到各节点被经过的次数，最后取最大值或输出各节点结果【8†L106-L110】【9†L148-L150】。其中题2需额外将最后一个终点的计数减去 1（因为最后到达不取糖）【9†L148-L150】，但差分思路一致。两题的核心都是“路径叠加，节点计数”。

- **题3（运输计划）**：则稍有不同，不是直接累计路径数量，而是优化选择一条边权变为 0 来最小化最大路径时间。这题结合了树上差分和二分思想。我们先计算所有计划的原始距离，并猜测一个时间阈值 $T$。对所有超过 $T$ 的计划应用树上差分（这时把每条“超时”计划视为路径，统计哪些边被经过）【6†L58-L62】。差分累加后，如果存在一条边被所有超时计划共用且这条边的权重足够大，则说明改造该边可以使所有计划时间 $\le T$【6†L58-L62】。通过二分不断尝试，得到最小可行的 $T$。因此题3本质上是**对路径的一个全局最优化判定**，用到树上差分帮助判断是否有单条边可同时影响所有关键路径。

**总结**：树上差分的核心是把“整条路径加（或减）”的问题转化为节点（或边）的差分更新。以上题目分别对应了点差分和结合条件的应用场景。题1/2 直接求路径经过次数，使用点差分模板【8†L106-L110】；题3 则在二分框架内用差分统计路径经过情况【6†L58-L62】。三题共同点是依赖树的 LCA 和一次后续 DFS 来汇总结果，但细节上针对题意有所差异（是否取终点、不取终点，是否考虑权值优化等）。通过对比可以看出，树上差分对路径操作问题提供了 $O(n+m)$ 级的高效求解方式，适合大规模查询。 

**参考资料**：题目内容来自 USACO、JLOI、NOIP 官方描述【1†L23-L31】【11†L13-L18】；树上差分详解参考 Plozia 博客【8†L106-L110】【9†L156-L159】；三题思路则常见于训练资料与题解【4†L53-L60】【6†L58-L62】等。