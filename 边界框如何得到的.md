
---
title: YOLOv3 边界框坐标解码原理
aliases:
  - YOLO坐标解码
  - YOLO预测框还原
  - YOLOv3边界框推导
tags:
  - 计算机视觉
  - 目标检测
  - YOLO
  - Anchor
  - 边界框回归
status: learning
---

## YOLOv3 边界框坐标解码原理

## 摘要

YOLOv3 的检测头不会直接输出最终的绝对像素坐标，而是为每个网格位置和每个 [[Anchor]] 输出一组相对参数。

对于一个候选预测框，网络主要输出：

$$
t_x,\ t_y,\ t_w,\ t_h,\ t_o
$$

其中：

- $t_x,t_y$：预测框中心点相对于所在网格的位置参数；
- $t_w,t_h$：预测框相对于 Anchor 宽高的缩放参数；
- $t_o$：目标存在性参数，即 Objectness；
- 另外还会输出各类别对应的分类参数。

这些网络输出需要经过解码，才能得到最终的预测框：

$$
(b_x,b_y,b_w,b_h)
$$

YOLOv3 的坐标解码过程可以概括为：

$$
\boxed{
\text{确定网格}
+
\text{计算中心偏移}
+
\text{缩放Anchor}
+
\text{转换为像素坐标}
}
$$

> [!summary]
> YOLOv3 不直接预测任意大小的绝对检测框，而是在一个网格位置和一个 Anchor 的基础上，学习中心位置偏移量与宽高缩放量。

---

## 核心概念

### 输入图像与特征图

假设YOLOv3的输入图像大小为：

$$
416\times416
$$

其中一个检测尺度对应的特征图大小为：

$$
13\times13
$$

特征图中的每个位置对应输入图像中的一块区域。

该检测尺度的步长 Stride 为：

$$
s=\frac{416}{13}=32
$$

也就是说，特征图每移动一个网格单位，对应输入图像中移动32个像素。

可以理解为：

$$
\text{特征图坐标}\times32
=
\text{输入图像像素坐标}
$$

---

### 网格坐标

假设某个预测框由特征图中的第 $c_x$ 列、第 $c_y$ 行负责。

其中：

- $c_x$：网格在水平方向上的整数索引；
- $c_y$：网格在垂直方向上的整数索引。

通常索引从0开始。

例如：

$$
c_x=5,\qquad c_y=6
$$

表示该预测来自特征图第5列、第6行的网格位置。

---

### Anchor先验框

[[Anchor]] 是训练前根据数据集中真实框宽高统计得到的一组代表性尺寸。

例如：

$$
(10,14),\quad(23,27),\quad(40,35)
$$

每个Anchor由宽度和高度组成：

$$
(p_w,p_h)
$$

其中：

- $p_w$：Anchor宽度；
- $p_h$：Anchor高度。

Anchor为模型提供一个初始尺寸参考。

模型不需要从零开始直接预测任意宽高，而只需要学习：

> 当前目标框相对于这个Anchor应该放大还是缩小。

---

## YOLOv3的完整坐标解码公式

YOLOv3经典边界框解码公式为：

$$
b_x=\sigma(t_x)+c_x
$$

$$
b_y=\sigma(t_y)+c_y
$$

$$
b_w=p_w e^{t_w}
$$

$$
b_h=p_h e^{t_h}
$$

其中：

- $\sigma(\cdot)$：Sigmoid函数；
- $t_x,t_y,t_w,t_h$：网络原始输出；
- $c_x,c_y$：当前网格的整数坐标；
- $p_w,p_h$：当前Anchor的宽高；
- $b_x,b_y$：预测框中心点坐标；
- $b_w,b_h$：预测框宽高。

> [!important]
> 严格来说，网络原始输出的 $t_x,t_y$ 并不天然位于 $(0,1)$。
>
> 它们需要经过Sigmoid函数：
>
> $$
> \sigma(z)=\frac{1}{1+e^{-z}}
> $$
>
> 才会被限制到 $(0,1)$。

---

## 一、预测框中心点坐标

### 为什么要使用相对网格偏移

如果网络直接预测整张图像范围内的中心坐标，预测空间会比较大。

YOLOv3将图像划分成网格后，让每个预测位置主要负责附近的目标中心。

因此，中心点被拆分成两部分：

$$
\text{中心点坐标}
=
\text{网格整数坐标}
+
\text{网格内部偏移}
$$

即：

$$
b_x=c_x+\sigma(t_x)
$$

$$
b_y=c_y+\sigma(t_y)
$$

其中：

$$
0<\sigma(t_x)<1
$$

$$
0<\sigma(t_y)<1
$$

这保证预测框中心点位于当前网格单元内部。

---

### 水平方向中心坐标

假设预测来自第 $c_x$ 列网格，网络输出为 $t_x$。

经过Sigmoid后：

$$
\Delta x=\sigma(t_x)
$$

则预测框中心在特征图坐标系中的横坐标为：

$$
b_x=c_x+\Delta x
$$

也就是：

$$
b_x=c_x+\sigma(t_x)
$$

---

### 垂直方向中心坐标

同理，预测框中心在特征图坐标系中的纵坐标为：

$$
b_y=c_y+\sigma(t_y)
$$

---

### 转换成输入图像像素坐标

特征图大小为：

$$
13\times13
$$

输入图像大小为：

$$
416\times416
$$

对应步长为：

$$
s=32
$$

因此，预测框中心的输入图像像素坐标为：

$$
x_{\text{pixel}}=b_xs
$$

$$
y_{\text{pixel}}=b_ys
$$

代入解码公式：

$$
x_{\text{pixel}}
=
\left(c_x+\sigma(t_x)\right)s
$$

$$
y_{\text{pixel}}
=
\left(c_y+\sigma(t_y)\right)s
$$

---

## 二、预测框宽高

### 宽高不是相对于网格预测的

预测框中心点相对于当前网格进行预测，但预测框宽高通常相对于Anchor进行预测。

网络输出：

$$
t_w,\qquad t_h
$$

分别控制Anchor宽度和高度的缩放比例。

YOLOv3使用指数函数进行解码：

$$
b_w=p_we^{t_w}
$$

$$
b_h=p_he^{t_h}
$$

---

### 指数函数的作用

指数函数始终满足：

$$
e^x>0
$$

因此：

$$
b_w>0,\qquad b_h>0
$$

这可以保证预测框宽高不会变成负数。

同时：

- 当 $t_w=0$ 时，$e^{t_w}=1$；
- 当 $t_w>0$ 时，预测框比Anchor更宽；
- 当 $t_w<0$ 时，预测框比Anchor更窄。

高度的变化方式相同。

---

### 不同参数对应的缩放关系

当：

$$
t_w=0
$$

有：

$$
b_w=p_we^0=p_w
$$

预测宽度等于Anchor宽度。

当：

$$
t_w=\ln2
$$

有：

$$
b_w=p_we^{\ln2}=2p_w
$$

预测宽度是Anchor宽度的2倍。

当：

$$
t_w=-\ln2
$$

有：

$$
b_w=p_we^{-\ln2}=\frac{1}{2}p_w
$$

预测宽度是Anchor宽度的一半。

因此，$t_w$ 和 $t_h$ 学习的不是绝对宽高，而是相对于Anchor的对数缩放量。

---

## 三、Anchor为什么能降低学习难度

假设数据集中目标框宽度可能从10像素变化到300像素。

如果让网络直接回归绝对宽度：

$$
w\in[10,300]
$$

网络需要学习较大的数值变化范围。

使用Anchor后，网络只需在一个接近真实目标尺寸的参考框上进行调整。

例如Anchor宽度为：

$$
p_w=80
$$

真实目标宽度为：

$$
w=100
$$

网络需要满足：

$$
100=80e^{t_w}
$$

因此：

$$
e^{t_w}=\frac{100}{80}=1.25
$$

$$
t_w=\ln1.25
$$

模型只需要学习一个较小的相对修正量，而不需要直接从零预测100像素。

> [!note]
> Anchor可以理解为检测框的初始模板。
>
> 网络学习的主要任务是：
>
> - 中心点向哪里偏移；
> - Anchor需要放大还是缩小；
> - 当前框中是否存在目标；
> - 该目标属于哪个类别。

---

## 四、目标存在性与类别预测

除了边界框参数，YOLO检测头还会输出：

- Objectness；
- 类别分数。

---

### Objectness

网络输出目标存在性参数：

$$
t_o
$$

经过Sigmoid后得到：

$$
P_{\text{obj}}=\sigma(t_o)
$$

其数值位于：

$$
0<P_{\text{obj}}<1
$$

它表示模型认为该预测框对应真实目标的置信程度。

可以近似理解为：

> 这个预测框中存在目标的可能性有多大。

---

### 类别概率

对于每个类别，检测头会输出相应的分类参数。

在YOLOv3中，各类别通常使用独立的Logistic分类器，并通过Sigmoid得到类别分数。

假设存在两个类别：

```text
bee
wasp
```

模型可能得到：

$$
P(\text{bee}\mid\text{object})=0.95
$$

$$
P(\text{wasp}\mid\text{object})=0.02
$$

最终某个类别的检测分数通常会综合：

$$
\text{类别检测分数}
=
P_{\text{obj}}
\times
P(\text{class}\mid\text{object})
$$

例如：

$$
P_{\text{obj}}=0.9
$$

$$
P(\text{bee}\mid\text{object})=0.95
$$

则蜜蜂类别的综合分数为：

$$
0.9\times0.95=0.855
$$

---

## 五、完整数值示例

假设：

- 输入图像大小为 $416\times416$；
- 特征图大小为 $13\times13$；
- 步长为 $32$；
- 当前网格位置为 $(c_x,c_y)=(5,6)$；
- 网络原始输出为：

$$
t_x=0,\qquad
t_y=0,\qquad
t_w=0,\qquad
t_h=0
$$

对应Anchor大小为：

$$
(p_w,p_h)=(80,60)
$$

---

### 计算中心点偏移

因为：

$$
\sigma(0)=0.5
$$

所以：

$$
b_x=c_x+\sigma(t_x)
$$

$$
b_x=5+0.5=5.5
$$

同理：

$$
b_y=6+0.5=6.5
$$

因此，预测框中心在特征图坐标系中为：

$$
(5.5,6.5)
$$

---

### 转换成像素坐标

步长为：

$$
s=32
$$

所以：

$$
x_{\text{pixel}}
=
5.5\times32
=
176
$$

$$
y_{\text{pixel}}
=
6.5\times32
=
208
$$

最终预测框中心点为：

$$
(176,208)
$$

---

### 计算预测框宽高

因为：

$$
t_w=0,\qquad t_h=0
$$

所以：

$$
e^{t_w}=e^0=1
$$

$$
e^{t_h}=e^0=1
$$

预测宽度为：

$$
b_w=p_we^{t_w}
$$

$$
b_w=80\times1=80
$$

预测高度为：

$$
b_h=p_he^{t_h}
$$

$$
b_h=60\times1=60
$$

最终预测框表示为：

$$
(x,y,w,h)=(176,208,80,60)
$$

---

### 转换为左上角和右下角坐标

很多绘图和评价程序需要使用：

$$
(x_1,y_1,x_2,y_2)
$$

已知中心点和宽高：

$$
x=176,\qquad y=208
$$

$$
w=80,\qquad h=60
$$

左上角坐标为：

$$
x_1=x-\frac{w}{2}
$$

$$
x_1=176-\frac{80}{2}=136
$$

$$
y_1=y-\frac{h}{2}
$$

$$
y_1=208-\frac{60}{2}=178
$$

右下角坐标为：

$$
x_2=x+\frac{w}{2}
$$

$$
x_2=176+\frac{80}{2}=216
$$

$$
y_2=y+\frac{h}{2}
$$

$$
y_2=208+\frac{60}{2}=238
$$

因此最终边界框为：

$$
(x_1,y_1,x_2,y_2)
=
(136,178,216,238)
$$

---

## 六、完整解码流程

一个YOLOv3候选框从网络输出到像素坐标的完整过程如下：

```text
检测头输出原始参数
        ↓
获得 tx、ty、tw、th
        ↓
对 tx、ty 使用 Sigmoid
        ↓
加上网格坐标 cx、cy
        ↓
乘以步长，得到像素中心坐标
        ↓
对 tw、th 使用指数函数
        ↓
乘以 Anchor 宽高
        ↓
得到预测框绝对宽高
        ↓
将中心点宽高格式转换为角点格式
```

用公式表示为：

$$
x_{\text{pixel}}
=
\left(c_x+\sigma(t_x)\right)s
$$

$$
y_{\text{pixel}}
=
\left(c_y+\sigma(t_y)\right)s
$$

$$
w_{\text{pixel}}
=
p_we^{t_w}
$$

$$
h_{\text{pixel}}
=
p_he^{t_h}
$$

随后：

$$
x_1=x_{\text{pixel}}-\frac{w_{\text{pixel}}}{2}
$$

$$
y_1=y_{\text{pixel}}-\frac{h_{\text{pixel}}}{2}
$$

$$
x_2=x_{\text{pixel}}+\frac{w_{\text{pixel}}}{2}
$$

$$
y_2=y_{\text{pixel}}+\frac{h_{\text{pixel}}}{2}
$$

---

## 七、多尺度预测

YOLOv3不会只使用 $13\times13$ 一个特征图，而是通常使用三个检测尺度：

$$
13\times13
$$

$$
26\times26
$$

$$
52\times52
$$

对于输入尺寸 $416\times416$，对应的步长分别为：

| 特征图尺寸 | 步长 | 主要负责目标 |
|---|---:|---|
| $13\times13$ | 32 | 较大目标 |
| $26\times26$ | 16 | 中等目标 |
| $52\times52$ | 8 | 较小目标 |

高分辨率特征图保留了更多空间细节，因此更适合检测小目标。

对于蜜蜂等小目标检测任务：

> $52\times52$ 等较高分辨率检测层通常非常重要。

相关笔记：

- [[多尺度特征融合]]
- [[FPN]]
- [[小目标检测]]
- [[特征图]]
- [[感受野]]

---

## 八、训练阶段与推理阶段的区别

### 训练阶段

训练阶段需要：

- 网络输出预测参数；
- 解码或使用等价的回归表示；
- 将预测与真实框进行匹配；
- 计算边界框损失；
- 计算Objectness损失；
- 计算分类损失；
- 执行[[反向传播]]；
- 更新模型参数。

### 推理阶段

推理阶段需要：

- 对网络输出进行解码；
- 过滤低置信度预测；
- 使用[[NMS]]删除重复框；
- 将框映射回原始图像坐标；
- 输出最终检测结果。

推理阶段不会执行：

- 损失计算；
- 反向传播；
- 参数更新。

---

## 九、常见误区

### 误区一：$t_x$ 和 $t_y$ 本身位于 $(0,1)$

不准确。

网络原始输出的 $t_x$ 和 $t_y$ 可以是任意实数。

真正位于 $(0,1)$ 的是：

$$
\sigma(t_x)
$$

和：

$$
\sigma(t_y)
$$

---

### 误区二：YOLO直接输出绝对像素坐标

错误。

YOLOv3检测头输出的是相对参数。

最终像素坐标需要结合：

- 网格位置；
- Sigmoid偏移；
- 特征图步长；
- Anchor尺寸；
- 指数缩放。

共同计算得到。

---

### 误区三：中心坐标和宽高都相对于网格

错误。

YOLOv3中：

- 中心点位置相对于网格进行预测；
- 宽高相对于Anchor进行预测。

即：

$$
\text{中心点参考网格}
$$

$$
\text{宽高参考Anchor}
$$

---

### 误区四：$t_w=0$意味着预测框宽度为0

错误。

当：

$$
t_w=0
$$

有：

$$
e^{t_w}=1
$$

因此：

$$
b_w=p_w
$$

预测框宽度等于Anchor宽度，而不是0。

---

### 误区五：Objectness等于最终类别置信度

不完全正确。

Objectness主要表示预测框中存在目标的置信程度。

最终某类别检测分数通常还需要结合类别分数：

$$
\text{score}
=
P_{\text{obj}}
\times
P(\text{class}\mid\text{object})
$$

---

### 误区六：所有YOLO版本都使用完全相同的解码公式

错误。

本文介绍的是经典YOLOv3的Anchor-based解码方式。

不同YOLO版本可能使用：

- 不同的中心点约束；
- 不同的宽高变换；
- 不同的标签分配方法；
- Anchor-free检测头；
- 不同的边界框分布回归方式。

例如现代部分YOLO模型不再依赖传统Anchor，而是采用Anchor-free检测方式。

因此在阅读具体代码时，需要确认：

- 模型版本；
- 检测头结构；
- 边界框编码方式；
- 解码函数实现。

---

## 核心总结

YOLOv3的边界框解码依赖四类信息：

1. 网络输出的相对参数；
2. 当前预测所在的网格位置；
3. 当前特征图对应的步长；
4. 与该预测对应的Anchor尺寸。

中心点解码公式为：

$$
b_x=c_x+\sigma(t_x)
$$

$$
b_y=c_y+\sigma(t_y)
$$

转换成像素坐标：

$$
x_{\text{pixel}}
=
\left(c_x+\sigma(t_x)\right)s
$$

$$
y_{\text{pixel}}
=
\left(c_y+\sigma(t_y)\right)s
$$

宽高解码公式为：

$$
w_{\text{pixel}}=p_we^{t_w}
$$

$$
h_{\text{pixel}}=p_he^{t_h}
$$

因此，YOLOv3的预测逻辑可以概括为：

$$
\boxed{
\text{找到负责网格}
+
\text{预测网格内偏移}
+
\text{缩放Anchor}
}
$$

这种设计的优势是：

- 将全图坐标预测转化为局部网格偏移预测；
- 通过Anchor降低宽高回归难度；
- 保证预测框宽高始终为正；
- 支持在不同尺度特征图上并行预测；
- 保留YOLO单阶段检测器的高计算效率。

> [!summary]
> YOLOv3不是直接“画出一个框”，而是先确定哪个网格负责目标，再预测目标中心在网格内的位置，同时根据Anchor调整检测框宽高，最后将结果转换为输入图像中的绝对像素坐标。

---

## 相关笔记

- [[YOLO]]
- [[YOLOv3]]
- [[目标检测]]
- [[边界框回归]]
- [[Anchor]]
- [[特征图]]
- [[Stride]]
- [[Sigmoid函数]]
- [[Objectness]]
- [[置信度]]
- [[多尺度特征融合]]
- [[小目标检测]]
- [[IoU]]
- [[NMS]]
- [[反向传播]]
