![[Pasted image 20260505163600.png]]
![[Pasted image 20260505165456.png]]


[[逆元]]1 \p在数论中，**逆元**（Modular Multiplicative Inverse，模乘法逆元）是一个非常重要的概念，尤其在模运算和密码学（如RSA算法）中应用广泛。

简单来说，**逆元**就是模运算下的“倒数”。

### 1. 从普通倒数到模逆元

在普通算术中，一个数 \( a \) 的倒数 \( a^{-1} \) 满足：
\[
a \times a^{-1} = 1
\]
例如，\( 2 \) 的倒数是 \( 0.5 \)，因为 \( 2 \times 0.5 = 1 \)。

但在**模运算**中，我们只关心整数，并且结果要在一个固定的范围内（比如 \( 0 \) 到 \( m-1 \)）。这时，我们定义：

**定义**：给定一个整数 \( a \) 和一个模数 \( m \)，如果存在一个整数 \( x \)，使得：
\[
a \times x \equiv 1 \pmod{m}
\]
那么，我们就称 \( x \) 是 \( a \) 在模 \( m \) 下的**乘法逆元**，通常记作 \( a^{-1} \pmod{m} \)。

### 2. 直观理解

- **普通倒数**：\( 2 \times 0.5 = 1 \)（结果是精确的1）。
- **模逆元**：比如在模 \( 7 \) 下，\( 2 \) 的逆元是 \( 4 \)，因为：
  \[
  2 \times 4 = 8 \equiv 1 \pmod{7}
  \]
  这里 \( 8 \) 除以 \( 7 \) 余 \( 1 \)，所以 \( 4 \) 就是 \( 2 \) 在模 \( 7 \) 下的逆元。

### 3. 逆元存在的条件

**并不是所有整数在任意模数下都有逆元**。逆元存在的充要条件是：

> **\( a \) 和模数 \( m \) 互质**，即 \( \gcd(a, m) = 1 \)。

- 如果 \( \gcd(a, m) = 1 \)，则逆元存在且唯一（在模 \( m \) 的意义下）。
- 如果 \( \gcd(a, m) > 1 \)，则逆元不存在。

**例子**：
- 模 \( 6 \) 下，\( 2 \) 没有逆元，因为 \( \gcd(2, 6) = 2 \neq 1 \)。（任何 \( 2 \times x \) 都是偶数，模 \( 6 \) 后只能是 \( 0, 2, 4 \)，不可能得到 \( 1 \)）
- 模 \( 6 \) 下，\( 5 \) 有逆元，因为 \( \gcd(5, 6) = 1 \)，且 \( 5 \times 5 = 25 \equiv 1 \pmod{6} \)，所以 \( 5 \) 的逆元是它自己。

### 4. 如何计算逆元？

有几种常见方法：

#### 方法一：扩展欧几里得算法（最通用）
这是求解逆元的标准算法。因为 \( a \times x \equiv 1 \pmod{m} \) 等价于存在整数 \( k \) 使得：
\[
a x + m k = 1
\]
这正是扩展欧几里得算法求解的线性丢番图方程。当 \( \gcd(a, m) = 1 \) 时，算法会给出 \( x \) 和 \( k \)，其中 \( x \) 就是逆元。

#### 方法二：费马小定理（当模数为质数时）
如果模数 \( p \) 是质数，且 \( a \) 不是 \( p \) 的倍数，那么：
\[
a^{p-1} \equiv 1 \pmod{p}
\]
两边同时除以 \( a \)（即乘以 \( a^{-1} \)），得到：
\[
a^{p-2} \equiv a^{-1} \pmod{p}
\]
所以逆元就是 \( a^{p-2} \bmod p \)。这可以用快速幂高效计算。

#### 方法三：暴力枚举（仅适用于小模数）
从 \( 1 \) 到 \( m-1 \) 逐个尝试，看哪个数 \( x \) 满足 \( a \times x \bmod m = 1 \)。效率很低，只适合教学或极小模数。

### 5. 逆元的用途

逆元在数论和密码学中非常关键，主要用途是**在模运算中实现“除法”**。

在普通算术中，除以一个数等于乘以它的倒数。在模运算中，我们不能直接做除法（因为结果可能不是整数），但我们可以**乘以这个数的逆元**。

例如：解同余方程 \( 3x \equiv 4 \pmod{7} \)
- 先求 \( 3 \) 在模 \( 7 \) 下的逆元：\( 3 \times 5 = 15 \equiv 1 \pmod{7} \)，所以逆元是 \( 5 \)。
- 两边乘以 \( 5 \)：\( 5 \times 3x \equiv 5 \times 4 \pmod{7} \)
- 左边：\( (5 \times 3)x \equiv 1 \times x \equiv x \pmod{7} \)
- 右边：\( 20 \equiv 6 \pmod{7} \)
- 所以解为 \( x \equiv 6 \pmod{7} \)。

### 总结

| 概念 | 普通算术 | 模运算 |
|------|----------|--------|
| 运算 | 除法 \( a / b \) | 乘以逆元 \( a \times b^{-1} \pmod{m} \) |
| 逆元定义 | \( a \times a^{-1} = 1 \) | \( a \times a^{-1} \equiv 1 \pmod{m} \) |
| 存在条件 | \( a \neq 0 \) | \( \gcd(a, m) = 1 \) |
| 计算方法 | 直接取倒数 | 扩展欧几里得 / 费马小定理 |

**一句话总结**：逆元就是模运算下的“倒数”，它让我们能在模世界里做“除法”，但前提是除数与模数互质。